# I2：配置与版本管理任务卡

> 历史交接记录：本页记录当时的实现及验证，不是当前使用入口。2026-09-17 用户已确认最小产品验证完成；精简后旧测试命令、数据和入口已移除。当前状态见 [迭代索引](../iteration-plan.md)，操作见 [项目入口](../../readme.md)。

> 历史任务：以下状态及证据对应旧基线，不表示服务拼图新需求已完成。新版实施按 [I6—I8 索引](../iteration-plan.md)执行。

- 状态：已完成 12/12 张；实际交付与验证见各卡交接记录。
- 需求映射：P01—P03、P07、P08、P09 配置声明。
- 执行规则与统一完成标准：[迭代总计划](../iteration-plan.md)。
- 必读：[协作约定](../../AGENT.md)、[需求](../product-requirements.md)、[架构](../architecture-design.md)。

每张卡只有一个主要交付目标；为该目标补充接口适配、错误处理和轻量验证仍属于同一任务。按显式依赖执行，不要求为了完成早期卡而提前实现后续能力。

<a id="i2-t01"></a>

## I2-T01 内存凭据仓储

- **目标**：交付可验证的“内存凭据仓储”能力。
- **前置任务**：I1-T04。
- **输入依据**：架构第 7、11.2 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：repositories/ 凭据引用仓储。
- **范围与约束**：只保存在进程内；普通读取只返回引用或掩码。
- **最小验收**：写入合成凭据并取得引用；配置响应和日志均不含原值；重启后不存在。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：src/agent_platform/repositories/credentials.py、checks/credentials.py。
  - 验证：`.venv/bin/python checks/credentials.py` → 引用及掩码读取、日志表示脱敏、会话清空断言通过。
  - 使用环境：本地已安装依赖与合成数据；未调用真实模型。
  - 未完成与阻塞：无；仅完成本卡范围。
  - 实现决定：SecretStr 保存原值；只有显式传输入口可取用，普通接口不返回原值。
  - 下一可执行任务：按索引显式依赖继续下一张未完成卡。

<a id="i2-t02"></a>

## I2-T02 环境管理接口

- **目标**：交付可验证的“环境管理接口”能力。
- **前置任务**：I2-T01。
- **输入依据**：架构第 7、10 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：环境创建、列表与更新接口。
- **范围与约束**：维护 environmentId/revision 和统一更新入口；任务占用接入留 I3-T02。
- **最小验收**：创建并更新合成环境，环境 revision 改变；非法连接参数有字段错误。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：src/agent_platform/contracts/environments.py、repositories/environments.py、application.py、checks/environments.py。
  - 验证：`.venv/bin/python checks/environments.py` → HTTP 创建与更新通过，revision 1→2；非法 URL 返回字段路径，失败不改环境。
  - 使用环境：本地已安装依赖与合成数据；未调用真实模型。
  - 未完成与阻塞：无；仅完成本卡范围。
  - 实现决定：GET/POST /api/v1/environments 与 PUT /api/v1/environments/{id}；connections 包含模型/API 地址、超时和凭据引用，写入支持 credential；样例见检查脚本。
  - 下一可执行任务：按索引显式依赖继续下一张未完成卡。

<a id="i2-t03"></a>

## I2-T03 环境配置页面

- **目标**：交付可验证的“环境配置页面”能力。
- **前置任务**：I2-T02、I1-T08。
- **输入依据**：架构第 11.2 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：desktop/renderer/ 环境表单。
- **范围与约束**：只编辑环境；扩展对应 IPC 方法，不实现实例配置页。
- **最小验收**：页面创建并修改环境；密钥掩码；失败响应显示具体原因。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：desktop/renderer/、desktop/preload/index.cjs、desktop/main/ipc.cjs、desktop/checks/environments.cjs。
  - 验证：`env -u ELECTRON_RUN_AS_NODE desktop/node_modules/.bin/electron desktop/checks/environments.cjs` → 真实页面创建、修改环境通过；密码框清空，非法 URL 显示 baseUrl 路径。
  - 使用环境：本地已安装依赖与合成数据；未调用真实模型。
  - 未完成与阻塞：无；仅完成本卡范围。
  - 实现决定：有限 IPC 白名单；支持多连接 JSON，凭据独立密码框输入。
  - 下一可执行任务：按索引显式依赖继续下一张未完成卡。

<a id="i2-t04"></a>

## I2-T04 通用块注册表

- **目标**：交付可验证的“通用块注册表”能力。
- **前置任务**：I1-T10、I1-T12。
- **输入依据**：架构第 5、6 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：registry/ 通用块声明、加载和绑定查询。
- **范围与约束**：只提供注册与可用性检查；块执行和 LMS 逻辑留 I3。
- **最小验收**：注册一个带输入输出契约的本地块；缺少入口时拒绝注册。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：src/agent_platform/contracts/blocks.py、registry/blocks.py、registry/validation.py、examples/configuration/block/、checks/blocks.py。
  - 验证：`.venv/bin/python checks/blocks.py` → 带严格输入输出契约的本地块注册及查询通过；缺入口拒绝。
  - 使用环境：本地已安装依赖与合成数据；未调用真实模型。
  - 未完成与阻塞：无；仅完成本卡范围。
  - 实现决定：块按 blockId/version 固定内存内容，同版本不同内容拒绝；不执行业务入口。
  - 下一可执行任务：按索引显式依赖继续下一张未完成卡。

<a id="i2-t05"></a>

## I2-T05 配置组合校验器

- **目标**：交付可验证的“配置组合校验器”能力。
- **前置任务**：I1-T11、I2-T02、I2-T04。
- **输入依据**：架构第 4、5、9.1 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：application/ 多包多配置校验函数。
- **范围与约束**：验证环境、包与块引用和作用域；显式生成预算默认值，不调用模型。
- **最小验收**：两包两配置有效；缺依赖或同作用域冲突被拒绝；全局预算默认值可解释。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：src/agent_platform/configuration.py、contracts/instances.py、registry/packages.py、examples/configuration/、checks/configuration.py。
  - 验证：`.venv/bin/python checks/configuration.py` → 两包两配置有效；缺依赖、作用域冲突、错误类型拒绝；默认 loop=4、token=200。
  - 使用环境：本地已安装依赖与合成数据；未调用真实模型。
  - 未完成与阻塞：无；仅完成本卡范围。
  - 实现决定：扩展 capabilityBindings 明确连接或固定块目标及契约；包配置显式规范化，全局预算缺省项按绑定求和；不调用模型。
  - 下一可执行任务：按索引显式依赖继续下一张未完成卡。

<a id="i2-t06"></a>

## I2-T06 实例快照保存

- **目标**：交付可验证的“实例快照保存”能力。
- **前置任务**：I2-T05、I1-T12、I1-T13。
- **输入依据**：架构第 6.1 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：versions/ 不可变实例快照仓储。
- **范围与约束**：保存入口、图源码、包、配置、契约、资源和块版本；不切换服务指针。
- **最小验收**：保存后修改原文件与配置对象，快照内容保持不变；不完整定义不入库。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：src/agent_platform/versions/snapshots.py、checks/instance_snapshots.py。
  - 验证：`.venv/bin/python checks/instance_snapshots.py` → 修改源文件和原配置不影响快照；图可执行；缺入口不入库。
  - 使用环境：本地已安装依赖与合成数据；未调用真实模型。
  - 未完成与阻塞：无；仅完成本卡范围。
  - 实现决定：保存完整字节、固定包/块、序列化配置与契约；图工厂使用 I1 SequentialExecutor；本卡不切换服务。
  - 下一可执行任务：按索引显式依赖继续下一张未完成卡。

<a id="i2-t07"></a>

## I2-T07 版本号分配

- **目标**：交付可验证的“版本号分配”能力。
- **前置任务**：I2-T06。
- **输入依据**：架构第 4 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：versions/ 版本分配器。
- **范围与约束**：只实现 revision、major/minor 和 changeKind；不添加发布流程。
- **最小验收**：配置变更为小版本，包或流程变更为大版本，整体变更标记 breaking；已有编号不复用。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：src/agent_platform/versions/numbering.py、checks/versions.py。
  - 验证：`.venv/bin/python checks/versions.py` → 首版 1.0、配置 1.1、源码 2.0、整体 breaking 3.0；回退后继续 3.1/revision5。
  - 使用环境：本地已安装依赖与合成数据；未调用真实模型。
  - 未完成与阻塞：无；仅完成本卡范围。
  - 实现决定：版本分配器按服务保留最高分配序列；分类断言使用合成源码变更替身；配置声明文件不算源码变更。
  - 下一可执行任务：按索引显式依赖继续下一张未完成卡。

<a id="i2-t08"></a>

## I2-T08 稳定服务入口管理

- **目标**：交付可验证的“稳定服务入口管理”能力。
- **前置任务**：I2-T07。
- **输入依据**：架构第 6、10 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：服务创建、保存新版本、列表与当前 Schema API。
- **范围与约束**：原子保存并切换 activeInstanceId；只提供当前解析函数，任务提交留 I3。
- **最小验收**：成功更新保持 serviceId 并改变实例；故意校验失败后当前指针不变。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：src/agent_platform/services.py、contracts/services.py、registry/definitions.py、application.py、checks/services.py。
  - 验证：`.venv/bin/python checks/services.py` → 服务 ID 保持不变，保存生成 1.1 新实例；故意缺入口失败后指针和历史不变；当前 Schema 正确。
  - 使用环境：本地已安装依赖与合成数据；未调用真实模型。
  - 未完成与阻塞：无；仅完成本卡范围。
  - 实现决定：新增服务创建/版本保存/列表/Schema API；请求含 name、definitionLoadId、definition，样例见 checks/services.py；同循环无 await 原子切换。
  - 下一可执行任务：按索引显式依赖继续下一张未完成卡。

<a id="i2-t09"></a>

## I2-T09 服务配置页面

- **目标**：交付可验证的“服务配置页面”能力。
- **前置任务**：I2-T08、I1-T08、I2-T12。
- **输入依据**：架构第 11.1 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：实例定义加载与配置保存表单。
- **范围与约束**：只实现创建及编辑；嵌套配置可用经校验的 JSON 编辑区，历史页单列。
- **最小验收**：页面保存两包两配置实例，显示稳定入口和当前版本；失败时显示字段路径。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：desktop/renderer/、desktop/main/ipc.cjs、desktop/preload/index.cjs、desktop/checks/services.cjs。
  - 验证：`env -u ELECTRON_RUN_AS_NODE desktop/node_modules/.bin/electron desktop/checks/services.cjs` → 真实页面加载两包两配置，显示求和预算并保存 1.0/1.1；稳定入口可见，错误显示 loopLimit 路径。
  - 使用环境：本地已安装依赖与合成数据；未调用真实模型。
  - 未完成与阻塞：无；仅完成本卡范围。
  - 实现决定：嵌套配置 JSON 由后端权威契约校验，页面展示 Schema、环境选择、加载句柄和当前版本。
  - 下一可执行任务：按索引显式依赖继续下一张未完成卡。

<a id="i2-t10"></a>

## I2-T10 历史版本回退接口

- **目标**：交付可验证的“历史版本回退接口”能力。
- **前置任务**：I2-T08。
- **输入依据**：架构第 6.2、10 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：历史列表和 activate API。
- **范围与约束**：重新选择原实例且校验当前环境；不新建回退版本、不恢复旧环境地址。
- **最小验收**：先保存 A/B 再选择 A，activeInstanceId 为原 A；环境不兼容时保持 B。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：src/agent_platform/services.py、contracts/services.py、application.py、checks/activation.py。
  - 验证：`.venv/bin/python checks/activation.py` → 保存 A/B 后选回原 A；环境仍为当前 URL；类型不兼容时回退拒绝并保持 B；历史仍两条。
  - 使用环境：本地已安装依赖与合成数据；未调用真实模型。
  - 未完成与阻塞：无；仅完成本卡范围。
  - 实现决定：历史 GET /services/{id}/versions；POST activate 只接收 instanceId，验证服务归属及当前环境；不分配新版本。
  - 下一可执行任务：按索引显式依赖继续下一张未完成卡。

<a id="i2-t11"></a>

## I2-T11 历史版本页面

- **目标**：交付可验证的“历史版本页面”能力。
- **前置任务**：I2-T10、I2-T09。
- **输入依据**：架构第 11.1 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：历史列表与回退操作。
- **范围与约束**：只展示当前会话历史与选择结果；不提供退役实例直接运行按钮。
- **最小验收**：从页面选择历史 A，稳定入口显示 A；退出重启后不出现旧历史。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：desktop/renderer/、desktop/main/ipc.cjs、desktop/preload/index.cjs、desktop/checks/services.cjs。
  - 验证：`env -u ELECTRON_RUN_AS_NODE desktop/node_modules/.bin/electron desktop/checks/services.cjs` → 真实历史按钮选回原 A，稳定入口显示 1.0；重建后端与窗口后旧服务和历史均为空。
  - 使用环境：本地已安装依赖与合成数据；未调用真实模型。
  - 未完成与阻塞：无；仅完成本卡范围。
  - 实现决定：历史只展示会话版本与选用操作，无退役实例运行按钮。
  - 下一可执行任务：按索引显式依赖继续下一张未完成卡。

<a id="i2-t12"></a>

## I2-T12 本地定义加载接口

- **目标**：交付可验证的“本地定义加载接口”能力。
- **前置任务**：I1-T12、I2-T04。
- **输入依据**：架构第 5、10 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：POST /registry/load 和加载结果契约。
- **范围与约束**：只将包、块及实例本地加载器接到 API，不执行业务或自动安装依赖。
- **最小验收**：提交有效本地路径返回可选择标识；缺入口与无效路径明确报错；返回中不暴露凭据。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：src/agent_platform/contracts/registry.py、registry/api.py、application.py、checks/registry_api.py。
  - 验证：`.venv/bin/python checks/registry_api.py` → HTTP 加载块、两个包及实例后成功创建服务；无效路径与缺入口返回明确字段且不泄露原异常。
  - 使用环境：本地已安装依赖与合成数据；未调用真实模型。
  - 未完成与阻塞：无；仅完成本卡范围。
  - 实现决定：POST /api/v1/registry/load 请求 kind/path，返回标识、Schema、实例句柄及显式预算默认值；不执行包/块业务或安装依赖。
  - 下一可执行任务：按索引显式依赖继续下一张未完成卡。

## 收尾修正记录

- 服务 Schema 响应补齐固定包和实例配置 Schema；真实 Electron 刷新页面再选服务后仍展示 `packages.echo` 的契约。
- 不存在的服务/实例返回 HTTP 404，同版本不同内容返回 409；页面展示稳定 serviceId 及实际可读取的 Schema 地址。
- 显式声明已安装的 `packaging` 直接依赖，执行 `uv lock --offline`，没有下载或安装依赖，没有改变已锁定包版本。
- 验证：`.venv/bin/python checks/services.py` 与 `env -u ELECTRON_RUN_AS_NODE desktop/node_modules/.bin/electron desktop/checks/services.cjs` 均通过；`git diff --check` 通过。
