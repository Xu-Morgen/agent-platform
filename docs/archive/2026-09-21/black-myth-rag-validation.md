# 知识库三题业务验收记录

日期：2026-09-21。真实自动验收已完成；用户于 2026-09-22 确认人工验收通过。原始文档及完整调用输入输出保存在授权本地知识库和任务历史，不提交 Git。模型为现有 ds/model（deepseek-flash），本轮上限为 30 次调用，失败与重试均计入。

## 正式资料与服务

7 份 AI 写作 DOCX 经正式页面导入知识库“黑神话材料阅读出题”（`kb_98bbca3582e344889c03e358da098542`）。主题为材料中的文化传播、技术创新及游戏产业影响；不把 AI 文稿当作现实事实权威。原始修订 `kr_c52fe64d0b974ea68c9a99cd71a9ba69`。通过文档替换控件将 360 文稿的同一原件重新保存为新版本，生成修订 `kr_c0fbda3e31ad4eec8c1f9b5d8f630d78`；仍有 7 份文档，原版本和旧任务证据保留。此次更新验证版本固定，不证明不同正文的检索质量。

服务“知识库三题出题 · ds”，`svc_303e8991d0124b4eb92de3fbc0b4edc3`。1.0 实例 `ins_1d3b30e2c0c7456a97c5908fb3bb0f74` 经正式页面保存、正常退出、重新启动后恢复且预检通过。

## 真实调用

| 场景 | 任务 | 次数 | 结果 |
| --- | --- | --- | --- |
| 1.0 正常请求 | `run_716265c1c694433db949155eb19dd423` | 3 | 失败；选用范围核验正确拒绝候选中未选用的片段，未交付题目 |

首个任务供应商报告 74,898 token。规划实际返回三题并识别文稿时间差异；单选生成引用了 `candidates` 中不属于 `selected` 的片段，单题核验拒绝。旧任务与登记证据仍引用原始修订，未随文档更新变化。

修复：Prompt 不再渲染完整 EvidenceContext，只提供 `selected` 的正文及必要规划/审查字段；全候选留在检索记录供追溯，不进入出题模型上下文。资源升级为 2.0.0（解析包保持 1.0.0）。块将关系校验错误作为带具体原因的 `CONTRACT_VALIDATION_ERROR` 返回，避免笼统子进程错误。针对真实失败输入，新的真实块子进程明确返回“引用必须来自本次固定修订的选用证据”。14 项离线业务检查及全部资源加载和接线复验通过。

## 验收界限

确定性检查和模型审题不能代替人工判断。真实修订、重出、质量不达标、资料冲突处理等分支须逐项记录是否触发。旧全文出题实验的 10 次调用不计入本实验结果或本轮授权次数。

## 自动验收结果（20 次调用）

| 场景 | 任务 | 次数 / token | 结果 |
| --- | --- | --- | --- |
| 2.0 正常请求，新文档修订 | `run_8020c33ecdfd41bfaba96d72ac75c12a` | 6 / 74,489 | `completed`，三题、三个 foreach 迭代及三类 switch 分支实际执行，首次整体审题 pass |
| 不支持的填空题请求 | `run_ef1118d28c694d67acb1a63ca4035d74` | 1 / 1,944 | `unsupported_request`，未检索或生成替代题型 |
| 无关主题、无匹配片段 | `run_71adee5c6bc940b3ab5e38e65c7f6960` | 2 / 3,037 | `insufficient_source`，选用 0 片段，明确三题缺项，不断言整个知识库无答案 |
| 3.0 替换为完整短语检索 | `run_7506469b51064ee68adc8975e80dbb23` | 6 / 65,399 | `completed`，三题生成与整体审题通过，仅替换普通检索资源并在页面保存 |
| 主题相关、缺少精确月度数据 | `run_e2792cbc320a4596aed68f09ea0e5b44` | 2 / 13,337 | `insufficient_source`，选用 30 片段仍不足，指出缺少 2025 月度销量/收入，并识别材料发布状态表述冲突 |

包含首次失败，本轮共 20 次实际模型调用、233,104 token，低于用户上限 30 次；retryLimit=0，没有隐藏连接测试或额外模型解释调用。任务创建时间及逐次用量保存在本地记录；未单独采集精确端到端耗时，不以模型调用耗时或轮询间隔伪造任务耗时。

2.0 正常请求的代理核对：单选 A 对应材料明确描述，其余三项与材料矛盾；判断 false 由引擎/图形技术突破的原文直接反证，附纠正；论述按产业研发、商业模式、市场竞争与行业标杆给出答案和五项评分点，合计 100 分。逐题引文均通过确定性选用范围及逐字核验。审题指出判断题第二条 AI 技术旁证不够直接，作为 advisory 保留，没有伪装为无任何问题。代理复核与人工验收分别记录；用户于 2026-09-22 明确回复“认可，人工验收通过”，确认本次三题评阅结果。

3.0 为替换检索试验实例 `ins_87f121c2c51148bba751c8425b69804b`。完成后通过页面恢复默认词面检索并补充可直接填入的输入样例，保存最终 4.0 实例 `ins_dc1fb98ee71f4e01bf44505fed955cc8`；包/块版本仍为上述 2.0.0，解析包 1.0.0。最终 4.0 未额外消耗模型调用，不能称作又一次独立真实生成验收。

## 逐项边界

- 已真实验证：默认及替换检索的主路径、三题型覆盖、false 判断与纠正、不支持题型、无匹配证据、相关主题资料不足、识别材料时间表述冲突、文档新修订与旧任务证据隔离。
- 已离线验证：伪造引用、漏题/重复题、重复选项、跨修订、未经审查交付、修订与重出共享两轮上限、定向修改边界、质量不达标、三种关键块的实际子进程。
- 未在本轮真实触发：revise、regenerate、两轮耗尽后的 quality_not_met、取消等待/传输失败、应用退出时存在运行任务、混入额外无关文档的检索排序、文档移除。空知识库仅以空检索的确定性边界覆盖。既有平台历史验收不能替代这些业务场景。
- 任务页实际检查：平台状态“已完成”与“业务状态：insufficient_source”分别显示，并提示需按业务状态判断是否满足要求。没有为黑神话新增平台业务执行分支。

完整记录保存于本地 `artifact/black-myth-rag-2026-09-21.json`（未纳入 Git），以及 PostgreSQL 的服务、任务和证据记录。原件仍在正式知识库存储中，Git 仅保留资源、说明及本摘要。

最终重启检查：4.0 服务、当前知识库修订和 2.0 成功任务跨桌面退出恢复，页面预检通过，业务状态正常显示。4.0 与已真实执行的 2.0 节点配置相同，流程仅增加输入样例；未额外执行模型调用。

## 2026-09-22 人工验收通过

用户明确回复：“认可，人工验收通过”。评阅对象为任务 `run_8020c33ecdfd41bfaba96d72ac75c12a` 的三题、答案、评分点与来源，整理稿位于本地 `artifact/black-myth-rag-human-review.md`。本次确认完成最后人工验收环节，未追加模型调用；累计仍为 20/30 次。真实修订、重出、取消等未触发场景仍按上述边界保留，不因人工主路径通过而改写为全部场景通过。
