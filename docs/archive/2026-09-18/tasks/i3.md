# I3：任务与能力执行任务卡

> 归档日期：2026-09-18。保留当时的需求、设计或交接记录；文中的完成状态、命令和接口可能已过时，不作为当前开发要求。当前入口见 [文档导航](../../../README.md)。

> 历史交接记录：本页记录当时的实现及验证，不是当前使用入口。2026-09-17 用户已确认最小产品验证完成；精简后旧测试命令、数据和入口已移除。当前状态见 [迭代索引](../../../README.md)，操作见 [项目入口](../../../../readme.md)。

> 历史任务：以下状态及证据对应旧基线，不表示服务拼图新需求已完成。新版实施按 [I6—I8 索引](../iteration-plan.md)执行。

- 状态：全部完成；已完成 13/13 张。
- 需求映射：P03—P08。
- 执行规则与统一完成标准：[迭代总计划](../iteration-plan.md)。
- 必读：[协作约定](../../../../AGENTS.md)、[需求](../product-requirements.md)、[架构](../architecture-design.md)。

每张卡只有一个主要交付目标；为该目标补充接口适配、错误处理和轻量验证仍属于同一任务。按显式依赖执行，不要求为了完成早期卡而提前实现后续能力。

<a id="i3-t01"></a>

## I3-T01 运行记录仓储

- **目标**：交付可验证的“运行记录仓储”能力。
- **前置任务**：I1-T04。
- **输入依据**：架构第 4、8、10 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：Run/StepRecord 模型及内存仓储。
- **范围与约束**：定义状态、实际版本、取消标志、错误与结果位置；不调度任务。
- **最小验收**：创建两条记录并独立更新；终态不可被普通更新覆盖；未知 runId 明确不存在。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：src/agent_platform/contracts/runs.py、repositories/runs.py、checks/runs.py。
  - 验证：`.venv/bin/python checks/runs.py` → 两记录独立更新，终态覆盖拒绝，未知记录 404。
  - 使用环境：本地已安装依赖与合成样例；未验收真实模型。
  - 未完成与阻塞：无；仅完成本卡范围。
  - 实现决定：固定输入及版本不可普通更新；finish 单次提交终态；未加入调度。
  - 下一可执行任务：按索引显式依赖继续下一张未完成卡。

<a id="i3-t02"></a>

## I3-T02 环境占用互斥

- **目标**：交付可验证的“环境占用互斥”能力。
- **前置任务**：I3-T01、I2-T02。
- **输入依据**：架构第 7 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：原子占用、释放和环境更新拒绝逻辑。
- **范围与约束**：排队起占用；同一临界区用于后续提交；无关环境可更新。
- **最小验收**：用人工登记的 runId 占用 A，更新 A 被拒绝、B 成功；释放后 A 可更新。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：src/agent_platform/repositories/environments.py、contracts/{environments,errors}.py、checks/occupancy.py。
  - 验证：`.venv/bin/python checks/occupancy.py` → 人工 runId 占用 A 后更新 409，B 更新成功，释放 A 后更新成功。
  - 使用环境：本地已安装依赖与合成样例；未验收真实模型。
  - 未完成与阻塞：无；仅完成本卡范围。
  - 实现决定：环境仓储 RLock 为提交共用临界区；公开 activeRunIds；写入拒绝发生在凭据写入之前。
  - 下一可执行任务：按索引显式依赖继续下一张未完成卡。

<a id="i3-t03"></a>

## I3-T03 任务提交接口

- **目标**：交付可验证的“任务提交接口”能力。
- **前置任务**：I3-T02、I2-T08。
- **输入依据**：架构第 7、10 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：POST /runs 和内存待执行队列。
- **范围与约束**：只受理并固定快照，不开始执行；输入错误不得创建任务。
- **最小验收**：同输入两次得不同 runId；错误输入 422、版本不符 409；提交后环境被占用。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：src/agent_platform/runtime/{submission,validation}.py、contracts/runs.py、application.py、checks/submission.py。
  - 验证：`.venv/bin/python checks/submission.py` → 相同输入生成不同 runId；错误输入 422、版本冲突 409，均未创建记录；排队占用环境。
  - 使用环境：本地已安装依赖与合成样例；未验收真实模型。
  - 未完成与阻塞：无；仅完成本卡范围。
  - 实现决定：POST /api/v1/runs 返回 202 Run；请求样例见 checks/submission.py；固定内存快照引用与独立环境副本。
  - 下一可执行任务：按索引显式依赖继续下一张未完成卡。

<a id="i3-t04"></a>

## I3-T04 状态流转检查入口

- **目标**：交付可验证的“状态流转检查入口”能力。
- **前置任务**：I3-T01、I1-T13。
- **输入依据**：架构第 5.3、8、9 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：runtime/ 唯一节点与终态检查入口。
- **范围与约束**：先实现显式契约检查和可注入预算/取消策略；未接策略时只供合成样例，不能宣称受限执行完成。
- **最小验收**：两节点图的开始、更新和终态都可观察到检查；异常终止仍进入清理路径。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：src/agent_platform/runtime/{boundary,graphs}.py、checks/boundary.py。
  - 验证：`.venv/bin/python checks/boundary.py` → 两节点开始/更新/边/终态可观察；合成策略拒绝后仍进入清理。
  - 使用环境：本地已安装依赖与合成样例；未验收真实模型。
  - 未完成与阻塞：无；仅完成本卡范围。
  - 实现决定：ContextVar 隔离每次执行策略；未装配预算或取消，只供合成执行；清理不受策略拦截。
  - 下一可执行任务：按索引显式依赖继续下一张未完成卡。

<a id="i3-t05"></a>

## I3-T05 图任务调度

- **目标**：交付可验证的“图任务调度”能力。
- **前置任务**：I3-T03、I3-T04。
- **输入依据**：架构第 2.2、8 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：单任务 worker 和终态清理。
- **范围与约束**：消费队列执行固定图；完成或失败均释放环境；不实现模型调用。
- **最小验收**：提交后断开 HTTP 仍执行；先提交 A 再切换 B，任务执行 A；失败后下个任务可执行。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：src/agent_platform/runtime/{worker,context}.py、application.py、examples/configuration/instance/entry.py、checks/worker.py。
  - 验证：`.venv/bin/python checks/worker.py` → 提交 HTTP 关闭后 worker 仍执行；切换 B 后原任务使用 A；合成启动失败后下一任务完成。
  - 使用环境：本地已安装依赖与合成样例；未验收真实模型。
  - 未完成与阻塞：无；仅完成本卡范围。
  - 实现决定：实例入口通过 context.run_graph 执行固定图；单 worker 生命周期由 FastAPI 管理；finally 释放环境；I4 在途退出待接入。
  - 下一可执行任务：按索引显式依赖继续下一张未完成卡。

<a id="i3-t06"></a>

## I3-T06 通用块执行入口

- **目标**：交付可验证的“通用块执行入口”能力。
- **前置任务**：I2-T04、I3-T04。
- **输入依据**：架构第 5.2、5.4 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：context.call_block 与块步骤记录。
- **范围与约束**：执行注册块并校验输入输出；不实现具体 LMS 规则。
- **最小验收**：运行合成块成功；非法输入和非法输出分别报对应阶段错误。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：src/agent_platform/runtime/context.py、checks/block_execution.py。
  - 验证：`.venv/bin/python checks/block_execution.py` → 注册块成功；非法输入/输出分别记录 input/output 阶段，步骤状态独立。
  - 使用环境：本地已安装依赖与合成样例；未验收真实模型。
  - 未完成与阻塞：无；仅完成本卡范围。
  - 实现决定：call_block 使用 packageBindingId.capabilityId 寻址固定块快照；输出替身仅用于触发非法类型验证。
  - 下一可执行任务：按索引显式依赖继续下一张未完成卡。

<a id="i3-t07"></a>

## I3-T07 LMS 响应解包块

- **目标**：交付可验证的“LMS 响应解包块”能力。
- **前置任务**：I3-T06。
- **输入依据**：架构第 5.4 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：blocks/ LMS 解包块及样例。
- **范围与约束**：只处理固定响应外壳，bizData 类型由能力契约决定。
- **最小验收**：成功码提取对象类型 bizData；失败码保留上游字段；错误 bizData 类型被拒绝。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：src/agent_platform/blocks/lms.py、contracts/errors.py、examples/execution/lms/、checks/lms.py。
  - 验证：`.venv/bin/python checks/lms.py` → 10000 提取对象；失败保留 code/msg/subCode/subMsg；错误 bizData 类型拒绝。
  - 使用环境：本地已安装依赖与合成样例；未验收真实模型。
  - 未完成与阻塞：无；仅完成本卡范围。
  - 实现决定：LMSResponse 仅定义外壳；unwrap 的目标契约由块绑定指定；只将已知外壳错误字段加入 details，不附带 bizData。
  - 下一可执行任务：按索引显式依赖继续下一张未完成卡。

<a id="i3-t08"></a>

## I3-T08 外部 API 调用入口

- **目标**：交付可验证的“外部 API 调用入口”能力。
- **前置任务**：I2-T02、I3-T06。
- **输入依据**：架构第 5.2、10 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：API 适配器和 context.call_api。
- **范围与约束**：显式地址、方法、认证引用、超时与响应校验；不自动重试，不做表达式映射编辑器。
- **最小验收**：本地响应服务验证成功和超时；将响应接入解包块，错误不转空成功。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：src/agent_platform/adapters/{api,http}.py、runtime/{context,worker}.py、contracts/instances.py、checks/{api_execution,local_http}.py、pyproject.toml、uv.lock。
  - 验证：`.venv/bin/python checks/api_execution.py && .venv/bin/python checks/activation.py` → 本地 HTTP 验证成功、超时及错误输出；响应接 LMS，业务失败未转成功；每次仅一请求；历史回退兼容校验通过。
  - 使用环境：本地已安装依赖与合成样例；未验收真实模型。
  - 未完成与阻塞：无；仅完成本卡范围。
  - 实现决定：API 绑定必须显式 apiMethod，可填相对 apiPath；凭据采用环境 credentialRef 的 Bearer 认证；httpx 已安装，仅离线更新直接依赖声明。
  - 下一可执行任务：按索引显式依赖继续下一张未完成卡。

<a id="i3-t09"></a>

## I3-T09 模型适配器协议

- **目标**：交付可验证的“模型适配器协议”能力。
- **前置任务**：I1-T04。
- **输入依据**：架构第 5.1、9.3 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：模型请求/响应契约、首个协议选择记录与能力声明。
- **范围与约束**：明确首期协议、模型输入输出、usage、输出限额、关闭传输和可靠计量条件；不实现多个供应商。
- **最小验收**：有效响应和缺少 usage 的响应均按声明处理；能力表区分精确、上界、估算与不支持，不承诺虚假严格保证。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：src/agent_platform/contracts/models.py、adapters/model_protocol.py、docs/protocols/model.md、checks/model_protocol.py。
  - 验证：`.venv/bin/python checks/model_protocol.py` → 有效响应保留供应商计数；缺 usage 标为 unsupported 且非零；声明区分精确/上界/估算/不支持。
  - 使用环境：本地已安装依赖与合成样例；未验收真实模型。
  - 未完成与阻塞：无；仅完成本卡范围。
  - 实现决定：首期选 Ollama 原生 /api/chat 非流式 JSON；供应商计量 exact 不代表完整 token 保证，严格总量能力为 false，I4 据此拒绝严格调用。
  - 下一可执行任务：按索引显式依赖继续下一张未完成卡。

<a id="i3-t10"></a>

## I3-T10 首个模型适配器

- **目标**：交付可验证的“首个模型适配器”能力。
- **前置任务**：I3-T09、I2-T02。
- **输入依据**：架构第 5.1、9.3 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：一个真实协议适配器和本地可控响应样例。
- **范围与约束**：实现请求、响应解析、超时与传输关闭；token 限额策略留 I4；真实密钥缺失不阻塞本地协议验证。
- **最小验收**：本地服务返回有效 JSON、畸形输出和超时，均得到明确结果；可关闭在途连接；不把本地替身记为真实模型验收。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：src/agent_platform/adapters/ollama.py、contracts/environments.py、checks/model_adapter.py。
  - 验证：`.venv/bin/python checks/model_adapter.py` → 本地 Ollama 协议替身验证有效 JSON、缺 usage、畸形、截断、超时；在途请求关闭后服务端观察断连且本地连接清空。
  - 使用环境：本地已安装依赖与合成样例；未验收真实模型。
  - 未完成与阻塞：无；仅完成本卡范围。
  - 实现决定：modelAdapter 为 ollama-chat；JSON 输出错误保留已知供应商 usage；仅关闭本地传输，不承诺远端停止计算；未做真实模型验收。
  - 下一可执行任务：按索引显式依赖继续下一张未完成卡。

<a id="i3-t11"></a>

## I3-T11 业务包调用入口

- **目标**：交付可验证的“业务包调用入口”能力。
- **前置任务**：I3-T10、I3-T04、I1-T10。
- **输入依据**：架构第 5.1、5.2 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：context.invoke_package 与一次尝试记录。
- **范围与约束**：包调用经过输入输出校验及统一策略入口；不自动重试、不允许包调度其他包。
- **最小验收**：两个模板包依次调用，结果可组合；输入或输出错误保留 bindingId 和 attempt。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：src/agent_platform/runtime/{context,packages,worker}.py、contracts/errors.py、examples/execution/model-{package,instance}/、checks/{package_execution,model_package}.py。
  - 验证：`.venv/bin/python checks/package_execution.py && .venv/bin/python checks/model_package.py` → 两模板包依次组合；输入/输出错误保留 bindingId/attempt；固定图到包到本地模型协议再到最终结果通过，usage 保留且环境释放。
  - 使用环境：本地已安装依赖与合成样例；未验收真实模型。
  - 未完成与阻塞：无；仅完成本卡范围。
  - 实现决定：实例 invoke_package 登记一次尝试；PackageContext 无包调度接口；模型必须经包声明能力进入；仅合成调用，loop/token 策略留 I4。
  - 下一可执行任务：按索引显式依赖继续下一张未完成卡。

<a id="i3-t12"></a>

## I3-T12 任务查询接口

- **目标**：交付可验证的“任务查询接口”能力。
- **前置任务**：I3-T05。
- **输入依据**：架构第 10 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：状态与结果查询 API。
- **范围与约束**：只读 Run 仓储；状态和最终结果是同一查询能力。
- **最小验收**：queued 返回状态但结果 409；completed 可取结果；failed 返回原因；未知 runId 为 404。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：src/agent_platform/runtime/queries.py、application.py、contracts/{runs,errors}.py、checks/run_queries.py。
  - 验证：`.venv/bin/python checks/run_queries.py` → queued 可查状态而结果 409；completed 可取结果；failed 保留原因与状态；未知 runId 两端点均 404。
  - 使用环境：本地已安装依赖与合成样例；未验收真实模型。
  - 未完成与阻塞：无；仅完成本卡范围。
  - 实现决定：结果响应为 runId/instanceId/result；未完成返回 RESULT_NOT_READY，失败沿用原错误并附 runStatus；查询不修改仓储。
  - 下一可执行任务：按索引显式依赖继续下一张未完成卡。

<a id="i3-t13"></a>

## I3-T13 桌面任务面板

- **目标**：交付可验证的“桌面任务面板”能力。
- **前置任务**：I3-T12、I2-T09。
- **输入依据**：架构第 11.1 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：输入、提交及任务详情面板。
- **范围与约束**：只接入已有任务 API，轮询状态；取消按钮等对应接口完成后接入。
- **最小验收**：页面提交合成图并看到完成结果；错误任务显示步骤和原因；刷新后可按 runId 查询。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：desktop/renderer/{index.html,index.js}、main/ipc.cjs、preload/index.cjs、contracts/、desktop/checks/runs.cjs、src/agent_platform/runtime/{context,graphs}.py。
  - 验证：`env -u ELECTRON_RUN_AS_NODE desktop/node_modules/.bin/electron desktop/checks/runs.cjs` → 真实 Electron 页面提交合成图并显示结果；非法节点输出展示步骤及原因；刷新后按 runId 查询成功；422 与 404 明确显示；另以 checks/graphs.py 和 checks/model_package.py 验证节点记录接线。
  - 使用环境：本地已安装依赖与合成样例；未验收真实模型。
  - 未完成与阻塞：无；仅完成本卡范围。
  - 实现决定：轮询 queued/running，终态停止轮询；提交携带 expectedInstanceId；输出仅用 textContent；不显示未实现的取消按钮；同步错误 Schema 与环境占用展示。
  - 下一可执行任务：按索引显式依赖继续下一张未完成卡。

## 收尾契约复核

- 模型能力绑定除包/实例双方一致外，还须匹配平台 `ModelRequest` / `ModelResponse`；避免两侧共同误声明后直到运行时才失败。
- `.venv/bin/python checks/model_binding.py` → 包/实例均误声明普通 Message 的配置在保存前返回 422，服务未创建。
- `.venv/bin/python checks/worker.py` → 事件屏障暂停运行任务时环境更新被拒绝；节点失败释放占用，下一任务可执行；全部结束后环境可更新。
- `.venv/bin/python checks/model_package.py` → 补充模型绑定检查后，固定图到包到本地协议替身的成功链路仍通过。
