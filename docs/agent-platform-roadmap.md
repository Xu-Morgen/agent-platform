# Agent Platform 产品定位与实施步骤

更新日期：2026-09-20。以用户最新确认的节点输入、Python 条件与出题扩展目标为依据；以下区分已交付能力和待开发计划。

## 产品边界

平台提供可组合的业务包、Python 通用块、独立契约、服务实例与运行记录。配置者定义完整流程、接线和执行边界；模型承担流程内约定的语义处理，输入输出均严格校验。

模型输出可包含契约声明的类型、枚举和业务字段。Python 脚本块读取这些数据，计算分支或循环的严格布尔条件；平台执行预先定义的下一步。模型不得编写或修改流程、脚本、接线或实例，模型输出不能被直接当作可执行配置。

例如：模型返回契约内允许的分类值，脚本检查分类并输出 bool，if/else 进入配置者已定义的处理分支。契约之外的分类、字段或类型应失败，不由平台猜测或自动补充流程。

## 第一步：桌面启动即持久化（已交付并验证）

桌面自动启动应用专用 PostgreSQL，内部生成连接和主密钥；适配器保存环境、加密凭据、资源源码、草稿、版本和运行历史；实例发布原子提交；重启将未完成任务标记为中断，不自动重放。任务页支持服务/状态筛选和分页列表，页面显示实际存储模式。启用方式及验收边界见 [持久化接入](persistence.md)。

验收包括源码文件删除后从快照重建、版本回退与编号连续、任务终态保留、未完成记录中断标记、凭据加密、数据库断连及单后端约束。已通过 10 项 Python 检查、4 项桌面生命周期检查及实际 Electron 启停验证；当前验收环境为 Ubuntu 24.04 / PostgreSQL 16.15。

## 第二步：节点输入机制与 Python 条件块（已交付并完成轻量验证）

[平台专项实施记录](archive/2026-09-20/node-input-and-python-conditions-plan.md)：节点统一接收 primary/references；简单模式自动保留上一节点主输入，高级模式选择合法作用域内的完整参考输出。if.condition 改为独立执行的 Python 通用块，与 while 一致，分支仍接收完整业务数据。同步契约、Prompt、编辑器、执行、作用域与不可变快照。

保留 FlowDraft 作为唯一实例入口和现有三类资源，但此次更新其协议；不保留旧输入和旧 if 的兼容执行路径，不静默修改历史。模型只通过严格契约输出数据，不编写代码或生成流程。P1–P4 已完成，当前协议为 flow-5；已验证纯块执行、严格边界、页面交互及隔离数据库重启，samples 已同步到 2.0.0。

## 第二步后续：文档出题质量与自主修订（已交付并验收）

按[出题扩展计划](archive/2026-09-20/document-question-quality-plan.md)交付“规划 → 初稿 → 审题 → 定向修订/重新出题 → 再审题 → 最终验收”。保留三类题各一道，默认最多修订两轮；AI 判断驱动选择与循环，Python 维护状态、校验依据与终止。本轮已按授权完成 ds 的 10 次真实调用、定向修订及用户人工评审，通过实际 Electron 页面保存正式服务并验证重启恢复；详见[验收记录](document-question-quality-validation.md)。未触发的真实重出题路径仅标为离线验证，既有 OCR 边界继续保留。

## 第三步：持续运行与扩展（后续事项）

2026-09-20 已先交付[固定版本服务组合与选择器优化](service-composition.md)：叶子实例可作为父流程节点，固定版本并共享任务生命周期；资源按类别搜索，契约按内容及校验规则归并展示。该能力为后续通用检索服务提供组合基础，本轮未实现 RAG。

完成备份恢复及引用安全清理后，测量队列等待、节点耗时与记录增长；再按实际负载确定队列上限、背压和租约。2026-09-21 已按用户要求提前交付单后端内的有限并发消费者池，配置见 [任务并发配置](../readme.md#任务并发配置)；不涉及多进程调度或任务恢复。权限、第三方代码沙箱、多 Agent、长期记忆和任务断点续跑需要独立需求，不由持久化隐式引入。

当前已安装并启用本地数据库，仍未交付跨平台安装包、备份管理或 LangGraph 检查点续跑。其他配置体验优化见 [产品细化计划](product-refinement.md)。
