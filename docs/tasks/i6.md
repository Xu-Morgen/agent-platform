# I6：拼图契约与配置基础

- 状态：未开始，0/7 张完成。
- 基线：2026-09-17 产品需求 v0.5、架构 v0.3；旧 I1—I5 仅作为可复用实现及历史证据。
- 必读：[需求](../product-requirements.md)、[设计](../architecture-design.md)、[迭代计划](../iteration-plan.md)、[协作约定](../../AGENT.md)。
- 完成规则：按迭代计划一次领取一张卡，回填真实文件、命令／步骤、结果、模型环境及未完成项；本文件的验收描述是目标，不能当作已执行证据。

<a id="i6-t01"></a>

## I6-T01 拼图与端口契约

- **目标**：交付拼图与端口契约。
- **前置任务**：I1-T03。
- **输入依据**：架构第 5.2、5.5 节；需求 P01—P09 及第 9 节确认记录中对应规则。
- **交付物**：FlowDraft、节点、端口引用、控制结构、服务输入输出及示例的单一 Pydantic 契约；导出 Schema 与最小协议样例。
- **范围与约束**：只定义结构及可定位错误，不编译执行；不兼容旧脚本实例入口。
- **最小验收**：顺序、if/else、repeat、while 描述可校验；未知类型、重复 nodeId、缺少条件／最大次数被拒绝；纯通用块流程允许不提供模型预算及环境。
- **验证方式**：局部样例或手动页面操作，涉及约束／计数时少量定向断言；实施后记录可复现命令或步骤，不预设未存在脚本，不要求全量测试、安装或打包。
- **交接记录**：状态未开始；实际文件及验证证据尚无。前置任务完成后领取，不以本次文档更新标记实现完成。

<a id="i6-t02"></a>

## I6-T02 单文件通用块加载

- **目标**：交付单文件通用块加载。
- **前置任务**：I6-T01、I1-T12。
- **输入依据**：架构第 5.6 节；需求 P01—P09 及第 9 节确认记录中对应规则。
- **交付物**：单文件装饰器／元数据协议、严格签名与契约读取、源码快照；字段转换、布尔条件、LMS 解包三个可运行示例。
- **范围与约束**：可声明类型与导入，但只一个注册业务函数；不要求 block.json；不安装新依赖。
- **最小验收**：加载 .py 文件得到用途及端口，不调用业务函数；零／多个注册函数、无输入输出声明失败；bool 输出拒绝字符串；源文件变更不改变已加载内容。
- **验证方式**：局部样例或手动页面操作，涉及约束／计数时少量定向断言；实施后记录可复现命令或步骤，不预设未存在脚本，不要求全量测试、安装或打包。
- **交接记录**：状态未开始；实际文件及验证证据尚无。前置任务完成后领取，不以本次文档更新标记实现完成。

<a id="i6-t03"></a>

## I6-T03 可拼接模块目录

- **目标**：交付可拼接模块目录。
- **前置任务**：I6-T02、I1-T10。
- **输入依据**：架构第 5.1、5.6、10 节；需求 P01—P09 及第 9 节确认记录中对应规则。
- **交付物**：包、单文件块和契约资源的加载／查询 API 及 IPC；稳定资源标识、版本与输入输出 Schema。
- **范围与约束**：加载不执行模块、不要求先创建实例；包配置合法性归节点配置。
- **最小验收**：无环境时仍可加载业务包并查询模块；目录返回块端口、包配置与能力要求；缺失代码依赖给出具体错误；列表不含凭据。
- **验证方式**：局部样例或手动页面操作，涉及约束／计数时少量定向断言；实施后记录可复现命令或步骤，不预设未存在脚本，不要求全量测试、安装或打包。
- **交接记录**：状态未开始；实际文件及验证证据尚无。前置任务完成后领取，不以本次文档更新标记实现完成。

<a id="i6-t04"></a>

## I6-T04 端口兼容与作用域校验

- **目标**：交付端口兼容与作用域校验。
- **前置任务**：I6-T01、I6-T03。
- **输入依据**：架构第 5.4、5.5 节；需求 P01—P09 及第 9 节确认记录中对应规则。
- **交付物**：出口到入口的保守可赋值校验；连接、必填输入、作用域、服务最终输出检查。
- **范围与约束**：前后端共享权威后端结果；不做隐式转换，不声称能静态执行任意自定义校验。
- **最小验收**：相同契约可接；缺字段、类型／可空／范围／额外字段不符拒绝；加入转换块后可接；前向引用、未执行分支和越作用域引用拒绝；分支共同出口及循环反馈检查通过。
- **验证方式**：局部样例或手动页面操作，涉及约束／计数时少量定向断言；实施后记录可复现命令或步骤，不预设未存在脚本，不要求全量测试、安装或打包。
- **交接记录**：状态未开始；实际文件及验证证据尚无。前置任务完成后领取，不以本次文档更新标记实现完成。

<a id="i6-t05"></a>

## I6-T05 业务包节点配置校验

- **目标**：交付业务包节点配置校验。
- **前置任务**：I6-T01、I6-T03、I2-T02。
- **输入依据**：架构第 5.7、7、9 节；需求 P01—P09 及第 9 节确认记录中对应规则。
- **交付物**：以 nodeId 为作用域的参数、局部预算、能力环境绑定及合法状态接口。
- **范围与约束**：模块资源可用与节点合法分开；环境占用和凭据边界沿用现有机制，不静默降级严格 token 模式。
- **最小验收**：同包两个节点可用不同参数、预算和环境；缺配置或错误连接类型无效；合法配置通过；纯通用块流程不要求模型配置。
- **验证方式**：局部样例或手动页面操作，涉及约束／计数时少量定向断言；实施后记录可复现命令或步骤，不预设未存在脚本，不要求全量测试、安装或打包。
- **交接记录**：状态未开始；实际文件及验证证据尚无。前置任务完成后领取，不以本次文档更新标记实现完成。

<a id="i6-t06"></a>

## I6-T06 具体校验错误与定位

- **目标**：交付具体校验错误与定位。
- **前置任务**：I6-T01、I1-T04。
- **输入依据**：架构第 5.7、10 节；需求 P01—P09 及第 9 节确认记录中对应规则。
- **交付物**：配置、连接及运行时统一 issue 模型和脱敏错误转换；节点、端口、字段路径、具体原因。
- **范围与约束**：修复上一轮第 4 项；尽量复用既有错误码，不能用通用提示抹掉原因。
- **最小验收**：数字代替字符串说明期望类型；缺项、重复索引保留具体原因；端口错误含源与目标；不输出学生文本、凭据或原始异常 ctx。
- **验证方式**：局部样例或手动页面操作，涉及约束／计数时少量定向断言；实施后记录可复现命令或步骤，不预设未存在脚本，不要求全量测试、安装或打包。
- **交接记录**：状态未开始；实际文件及验证证据尚无。前置任务完成后领取，不以本次文档更新标记实现完成。

<a id="i6-t07"></a>

## I6-T07 服务草稿与全流程预检

- **目标**：交付服务草稿与全流程预检。
- **前置任务**：I6-T04、I6-T05、I6-T06。
- **输入依据**：架构第 5.2、6、10 节；需求 P01—P09 及第 9 节确认记录中对应规则。
- **交付物**：同会话内存草稿保存读取、流程校验 API；完整定位结果与有效样例检查。
- **范围与约束**：不创建版本或任务；重启不恢复草稿；预检不调用模型。
- **最小验收**：不完整草稿可保存但没有 instanceId、不出现在可调用服务中；合法流程预检通过；纯块流程通过；样例违反输入契约时说明原因。
- **验证方式**：局部样例或手动页面操作，涉及约束／计数时少量定向断言；实施后记录可复现命令或步骤，不预设未存在脚本，不要求全量测试、安装或打包。
- **交接记录**：状态未开始；实际文件及验证证据尚无。前置任务完成后领取，不以本次文档更新标记实现完成。

