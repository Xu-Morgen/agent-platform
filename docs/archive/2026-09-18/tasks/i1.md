# I1：工程与协议基础任务卡

> 归档日期：2026-09-18。保留当时的需求、设计或交接记录；文中的完成状态、命令和接口可能已过时，不作为当前开发要求。当前入口见 [文档导航](../../../README.md)。

> 历史交接记录：本页记录当时的实现及验证，不是当前使用入口。2026-09-17 用户已确认最小产品验证完成；精简后旧测试命令、数据和入口已移除。当前状态见 [迭代索引](../../../README.md)，操作见 [项目入口](../../../../readme.md)。

> 历史任务：以下状态及证据对应旧基线，不表示服务拼图新需求已完成。新版实施按 [I6—I8 索引](../iteration-plan.md)执行。

- 状态：已完成 13/13 张任务卡；详细状态及证据见各卡交接记录。
- 需求映射：P01、P05、P06。
- 执行规则与统一完成标准：[迭代总计划](../iteration-plan.md)。
- 必读：[协作约定](../../../../AGENT.md)、[需求](../product-requirements.md)、[架构](../architecture-design.md)。

每张卡只有一个主要交付目标；为该目标补充接口适配、错误处理和轻量验证仍属于同一任务。按显式依赖执行，不要求为了完成早期卡而提前实现后续能力。

<a id="i1-t01"></a>

## I1-T01 Python 工程入口

- **目标**：交付可验证的“Python 工程入口”能力。
- **前置任务**：—。
- **输入依据**：架构第 2 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：pyproject.toml、Python 锁文件及后端启动入口。
- **范围与约束**：仅后端工程；依赖安装沿用既有授权规则，不创建 Electron 工程或发布产物。
- **最小验收**：使用选定 Python 环境启动最小后端入口，记录版本和启动命令。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：pyproject.toml、uv.lock、src/agent_platform/{application,__main__,__init__}.py、.gitignore。
  - 验证：项目 .venv 中真实启动 .venv/bin/python -m agent_platform --port <空闲端口>，GET /openapi.json 返回 200，发送 SIGTERM 后进程退出（返回码 -15）；FastAPI 0.141.1、Pydantic 2.13.5、Uvicorn 0.53.0。
  - 使用环境：Python 3.12.3、项目 `.venv` 与 `uv.lock`；桌面依赖见 `desktop/package-lock.json`；均为本地运行，不涉及真实模型。
  - 未完成与阻塞：无；仅完成本卡范围。
  - 实现决定：src 布局、应用工厂、默认 127.0.0.1:8000、单 worker；用户已完成依赖安装。
  - 下一可执行任务：按索引中显式依赖选择未完成卡。

<a id="i1-t02"></a>

## I1-T02 Electron 工程入口

- **目标**：交付可验证的“Electron 工程入口”能力。
- **前置任务**：—。
- **输入依据**：架构第 2 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：desktop/package.json、锁文件和最小窗口入口。
- **范围与约束**：仅桌面工程；不启动 Python，不引入大型前端框架。
- **最小验收**：启动一个本地 HTML 窗口，关闭后 Electron 进程退出。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：desktop/package.json、package-lock.json、main/、renderer/、checks/window.cjs。
  - 验证：env -u ELECTRON_RUN_AS_NODE npm --prefix desktop run check:window → 真实窗口加载本地 HTML、页面无 require，关闭后 Electron 退出码 0。
  - 使用环境：Python 3.12.3、项目 `.venv` 与 `uv.lock`；桌面依赖见 `desktop/package-lock.json`；均为本地运行，不涉及真实模型。
  - 未完成与阻塞：无；仅完成本卡范围。
  - 实现决定：原生 HTML/CSS；隔离上下文、关闭 Node 集成；最后窗口关闭即退出；IDE 环境需清除 ELECTRON_RUN_AS_NODE。
  - 下一可执行任务：按索引中显式依赖选择未完成卡。

<a id="i1-t03"></a>

## I1-T03 严格契约基础

- **目标**：交付可验证的“严格契约基础”能力。
- **前置任务**：I1-T01。
- **输入依据**：架构第 5.4 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：contracts/ 严格模型基类及 Schema 导出函数。
- **范围与约束**：只建立共同校验规则，不定义全部业务模型。
- **最小验收**：一个嵌套有效对象通过；未知字段和数字字符串被拒绝；导出 Schema 对应同一模型。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：src/agent_platform/contracts/{base,__init__}.py、checks/contracts.py。
  - 验证：.venv/bin/python checks/contracts.py → 嵌套有效对象通过；根与嵌套未知字段、数字字符串拒绝，字段路径正确；同模型导出 Schema 断言通过。
  - 使用环境：Python 3.12.3、项目 `.venv` 与 `uv.lock`；桌面依赖见 `desktop/package-lock.json`；均为本地运行，不涉及真实模型。
  - 未完成与阻塞：无；仅完成本卡范围。
  - 实现决定：Python snake_case、JSON camelCase；所有嵌套契约继承 StrictModel，默认值与赋值也校验。
  - 下一可执行任务：按索引中显式依赖选择未完成卡。

<a id="i1-t04"></a>

## I1-T04 统一错误契约

- **目标**：交付可验证的“统一错误契约”能力。
- **前置任务**：I1-T03。
- **输入依据**：架构第 10 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：contracts/ 错误模型与 FastAPI 错误转换。
- **范围与约束**：统一类别、阶段、原因和字段路径；不实现各外部适配器。
- **最小验收**：触发一条嵌套输入错误，响应保留路径；上游样例含认证值时输出不含该值。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：contracts/errors.py、http_errors.py、application.py、checks/errors.py。
  - 验证：.venv/bin/python checks/errors.py → ASGI HTTP 422 保留 body.items[0].count；上游认证值样例转换为 502 且不泄露认证文本。
  - 使用环境：Python 3.12.3、项目 `.venv` 与 `uv.lock`；桌面依赖见 `desktop/package-lock.json`；均为本地运行，不涉及真实模型。
  - 未完成与阻塞：无；仅完成本卡范围。
  - 实现决定：错误 JSON 固定 code/stage/message/runId/fieldPath/details；details 仅接收白名单类型字段；校验异常不透传 input/ctx/msg。
  - 下一可执行任务：按索引中显式依赖选择未完成卡。

<a id="i1-t05"></a>

## I1-T05 后端健康端点

- **目标**：交付可验证的“后端健康端点”能力。
- **前置任务**：I1-T04。
- **输入依据**：架构第 2.2、10 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：FastAPI /api/v1/health 及生命周期入口。
- **范围与约束**：只提供后端就绪状态，不实现任务 API。
- **最小验收**：启动后 GET 返回就绪；关闭后端后端口释放。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：application.py、contracts/health.py、checks/health.py。
  - 验证：.venv/bin/python checks/health.py → 真实后端 GET /api/v1/health 返回 {status: ready}，停止后同端口可重新绑定。
  - 使用环境：Python 3.12.3、项目 `.venv` 与 `uv.lock`；桌面依赖见 `desktop/package-lock.json`；均为本地运行，不涉及真实模型。
  - 未完成与阻塞：无；仅完成本卡范围。
  - 实现决定：lifespan 进入置 ready，退出清除；健康端点输出由严格模型校验；尚无任务 API。
  - 下一可执行任务：按索引中显式依赖选择未完成卡。

<a id="i1-t06"></a>

## I1-T06 父子进程控制协议

- **目标**：交付可验证的“父子进程控制协议”能力。
- **前置任务**：I1-T05。
- **输入依据**：架构第 2.2、8.2 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：contracts/ 控制消息规范与 JSON 样例。
- **范围与约束**：固定 ready 地址、startupError、shutdown、控制管道 EOF 和协议版本；不实现进程管理。
- **最小验收**：用一个轻量校验入口检查三类有效消息和一个未知消息；控制通道与普通日志分开。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：contracts/control.py、examples/control/、checks/control.py。
  - 验证：.venv/bin/python checks/control.py → 三类有效消息通过，未知类型、缺版本、布尔版本拒绝。
  - 使用环境：Python 3.12.3、项目 `.venv` 与 `uv.lock`；桌面依赖见 `desktop/package-lock.json`；均为本地运行，不涉及真实模型。
  - 未完成与阻塞：无；仅完成本卡范围。
  - 实现决定：JSON Lines v1；版本与消息类型为严格模型必填字段，桌面 Schema 直接导出；stdin 为专用父到子控制，fd 3 为专用子到父控制；stdout/stderr 日志分离；EOF 等同 shutdown。
  - 下一可执行任务：按索引中显式依赖选择未完成卡。

<a id="i1-t07"></a>

## I1-T07 后端子进程启动

- **目标**：交付可验证的“后端子进程启动”能力。
- **前置任务**：I1-T02、I1-T06。
- **输入依据**：架构第 2.2 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：desktop/main/ 后端启动器。
- **范围与约束**：仅启动与就绪握手；不实现任务页面，退出清理由下一卡负责。
- **最小验收**：Electron 启动唯一后端；收到 ready 并健康检查成功才开放操作；启动失败显示原因。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：server.py、__main__.py、desktop/main/{backend,desktop,index,validate}.cjs、desktop/contracts/、scripts/export_contracts.py、desktop/checks/backend.cjs。
  - 验证：env -u ELECTRON_RUN_AS_NODE desktop/node_modules/.bin/electron desktop/checks/backend.cjs → 真实 Electron 启动唯一 Python 子进程、ready 后健康检查成功；缺失解释器经过实际桌面入口显示启动失败；测试主动清理子进程。
  - 使用环境：Python 3.12.3、项目 `.venv` 与 `uv.lock`；桌面依赖见 `desktop/package-lock.json`；均为本地运行，不涉及真实模型。
  - 未完成与阻塞：无；仅完成本卡范围。
  - 实现决定：桌面默认申请随机端口；重复 start 共享同一 Promise；控制消息使用 Python 导出 Schema 校验；启动超时 15 秒；退出处理已由 I1-T09 接入。
  - 下一可执行任务：按索引中显式依赖选择未完成卡。

<a id="i1-t08"></a>

## I1-T08 桌面 IPC 请求桥

- **目标**：交付可验证的“桌面 IPC 请求桥”能力。
- **前置任务**：I1-T07。
- **输入依据**：架构第 2.2 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：desktop/preload/ 有限请求方法与主进程转发。
- **范围与约束**：只桥接健康请求并约定错误映射；不开放任意 Node 调用。
- **最小验收**：页面经 preload 获取健康状态；刷新页面后后端 PID 不变。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：desktop/preload/index.cjs、main/ipc.cjs、renderer/index.js、main/{window,desktop}.cjs、checks/ipc.cjs。
  - 验证：env -u ELECTRON_RUN_AS_NODE desktop/node_modules/.bin/electron desktop/checks/ipc.cjs → 真实页面通过 preload 读取健康状态，刷新后 PID 不变，后端退出返回 BACKEND_UNAVAILABLE；重跑 backend.cjs 启动错误展示通过。
  - 使用环境：Python 3.12.3、项目 `.venv` 与 `uv.lock`；桌面依赖见 `desktop/package-lock.json`；均为本地运行，不涉及真实模型。
  - 未完成与阻塞：无；仅完成本卡范围。
  - 实现决定：页面仅暴露 health 方法；主进程固定健康路由并验证同源主框架；IPC 使用 ok/data 或 ok/error，error 遵循 Python 导出契约。
  - 下一可执行任务：按索引中显式依赖选择未完成卡。

<a id="i1-t09"></a>

## I1-T09 空闲应用退出

- **目标**：交付可验证的“空闲应用退出”能力。
- **前置任务**：I1-T07。
- **输入依据**：架构第 8.2 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：主进程退出处理和后端控制管道关闭处理。
- **范围与约束**：仅无业务任务的进程清理；在途模型清理留 I4-T10。
- **最小验收**：关闭主窗口后后端和端口均释放；单独终止父进程后控制管道断开使后端退出。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：server.py、desktop/main/{backend,index}.cjs、desktop/checks/lifecycle*.cjs。
  - 验证：node desktop/checks/lifecycle.cjs → 真实桌面最后窗口关闭后后端 PID 消失、端口可绑定；SIGKILL 父进程后 stdin EOF 触发相同清理；node desktop/checks/startup.cjs 验证端口占用启动错误与启动期间退出不恢复就绪。
  - 使用环境：Python 3.12.3、项目 `.venv` 与 `uv.lock`；桌面依赖见 `desktop/package-lock.json`；均为本地运行，不涉及真实模型。
  - 未完成与阻塞：无；仅完成本卡范围。
  - 实现决定：退出先禁止新启动并清除地址，发送 shutdown/关闭 stdin，2 秒未退出则 SIGKILL；后端控制 EOF、畸形消息均停止服务；当前仅空闲任务范围。
  - 下一可执行任务：按索引中显式依赖选择未完成卡。

<a id="i1-t10"></a>

## I1-T10 业务包声明契约

- **目标**：交付可验证的“业务包声明契约”能力。
- **前置任务**：I1-T03。
- **输入依据**：架构第 5.1、9.1 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：包清单模型、预算配置模型和最小清单样例。
- **范围与约束**：声明包入口、模型、能力与 loop/token 字段；不执行包、不编排实例。
- **最小验收**：有效清单通过；缺入口、非法预算各返回具体字段错误。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：contracts/packages.py、examples/declarations/package.json、checks/packages.py。
  - 验证：.venv/bin/python checks/packages.py → 有效清单通过，缺入口及 0/负数/数字字符串/布尔预算拒绝并保留路径，预算 Schema 与严格模式默认值断言通过。
  - 使用环境：Python 3.12.3、项目 `.venv` 与 `uv.lock`；桌面依赖见 `desktop/package-lock.json`；均为本地运行，不涉及真实模型。
  - 未完成与阻塞：无；仅完成本卡范围。
  - 实现决定：引用格式 module:Symbol；包配置模型应继承 PackageBudget；包预算默认 1/32768、实例 strictTokenLimit 默认 true；样例仅为声明，不代表入口已执行。
  - 下一可执行任务：按索引中显式依赖选择未完成卡。

<a id="i1-t11"></a>

## I1-T11 实例声明契约

- **目标**：交付可验证的“实例声明契约”能力。
- **前置任务**：I1-T10。
- **输入依据**：架构第 4、5.2 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：实例定义模型与一包一配置样例。
- **范围与约束**：声明入口、图定义引用、包与配置绑定和环境引用；不实现保存版本。
- **最小验收**：有效实例通过；零包、零配置、重复作用域字段被拒绝。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：contracts/instances.py、examples/declarations/{instance.json,README.md}、checks/instances.py。
  - 验证：.venv/bin/python checks/instances.py → 一包一配置通过；零包、零配置、重复作用域字段、未知绑定作用域均拒绝并保留具体路径。
  - 使用环境：Python 3.12.3、项目 `.venv` 与 `uv.lock`；桌面依赖见 `desktop/package-lock.json`；均为本地运行，不涉及真实模型。
  - 未完成与阻塞：无；仅完成本卡范围。
  - 实现决定：包固定 version、配置固定 revision；作用域绑定到 bindingId；实例入口/图/输入输出均为符号引用，环境仅保存标识；版本保存与能力兼容校验留后续卡。
  - 下一可执行任务：按索引中显式依赖选择未完成卡。

<a id="i1-t12"></a>

## I1-T12 内容快照加载器

- **目标**：交付可验证的“内容快照加载器”能力。
- **前置任务**：I1-T11。
- **输入依据**：架构第 6 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：registry/ 源码与资源快照加载器。
- **范围与约束**：仅内存内容与独立模块命名空间；不实现服务版本管理或宿主依赖升级。
- **最小验收**：先加载 A 再修改源文件加载 B，两者执行不同预期；A 的 Prompt 资源仍为原内容。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：registry/{__init__,snapshots}.py、checks/snapshots.py。
  - 验证：.venv/bin/python checks/snapshots.py → 加载 A、修改源文件加载 B 后删除源目录；两者代码及延迟导入分别执行旧/新结果，A Prompt 保持原内容；只读快照断言通过。
  - 使用环境：Python 3.12.3、项目 `.venv` 与 `uv.lock`；桌面依赖见 `desktop/package-lock.json`；均为本地运行，不涉及真实模型。
  - 未完成与阻塞：无；仅完成本卡范围。
  - 实现决定：捕获源码与资源字节，摘要+独立命名空间；自有源码须相对导入，资源经 read_resource 读取；外部依赖沿用会话环境；显式 close 仅在无使用者时清理，不实现服务版本管理。
  - 下一可执行任务：按索引中显式依赖选择未完成卡。

<a id="i1-t13"></a>

## I1-T13 最小 LangGraph 执行器

- **目标**：交付可验证的“最小 LangGraph 执行器”能力。
- **前置任务**：I1-T11。
- **输入依据**：架构第 5.3 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：runtime/ 图构造入口及两节点样例。
- **范围与约束**：仅顺序图和独立状态，节点边界显式校验；不调用模型，不实现预算。
- **最小验收**：两次运行状态不串用；无效节点更新被拒绝；关闭 checkpointer 和自动重试。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：runtime/{__init__,graphs}.py、examples/workflow/two_nodes.py、checks/graphs.py。
  - 验证：.venv/bin/python checks/graphs.py → 真实 LangGraph 两节点的两次运行互不串状态，错误类型/未知字段/非字典更新拒绝，失败节点仅调用一次且无 checkpointer；two_nodes.py 输出 EXAMPLE 与两步事件。
  - 使用环境：Python 3.12.3、项目 `.venv` 与 `uv.lock`；桌面依赖见 `desktop/package-lock.json`；均为本地运行，不涉及真实模型。
  - 未完成与阻塞：无；仅完成本卡范围。
  - 实现决定：节点接收独立严格模型，返回 snake_case 部分更新，合并后显式校验；不配置持久化、缓存、重试、恢复或预算；图步数限制独立于包 loop。
  - 下一可执行任务：按索引中显式依赖选择未完成卡。
