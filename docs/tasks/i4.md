# I4：预算与终止行为任务卡

- 状态：已完成 3/12；其余按依赖继续。
- 需求映射：P03、P05—P09。
- 执行规则与统一完成标准：[迭代总计划](../iteration-plan.md)。
- 必读：[协作约定](../../AGENT.md)、[需求](../product-requirements.md)、[架构](../architecture-design.md)。

每张卡只有一个主要交付目标；为该目标补充接口适配、错误处理和轻量验证仍属于同一任务。按显式依赖执行，不要求为了完成早期卡而提前实现后续能力。

<a id="i4-t01"></a>

## I4-T01 loop 计数策略

- **目标**：交付可验证的“loop 计数策略”能力。
- **前置任务**：I3-T11。
- **输入依据**：架构第 9.1、9.2 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：局部/全局 loop 账本与调用前检查。
- **范围与约束**：只计包调用尝试，通用块及图步数不计；错误尝试不退还。
- **最小验收**：limit=1 第一次失败仍计 1，第二次入口尝试被拒绝；成功用满后允许最终报告；两绑定累计到全局。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：src/agent_platform/runtime/budgets.py、checks/loop_budget.py。
  - 验证：`.venv/bin/python checks/loop_budget.py` → limit=1 首次输入失败仍计 1，第二次拒绝；两绑定累计全局 2，满额允许终态；通用块不计数。
  - 使用环境：本地合成包，未调用真实模型。
  - 未完成与阻塞：无。
  - 实现决定：LoopPolicy 在仓储锁内原子登记局部/全局账；通过 Boundary 注入，worker 默认装配留 I4-T05。
  - 下一可执行任务：I4-T02，依赖已满足。

<a id="i4-t02"></a>

## I4-T02 token 用量账本

- **目标**：交付可验证的“token 用量账本”能力。
- **前置任务**：I3-T09。
- **输入依据**：架构第 9.3 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：输入输出累计、来源标记及局部/全局账本。
- **范围与约束**：只归一化计量，不实施严格/非严格阻止策略。
- **最小验收**：给定两次 usage 后总量正确；无 usage 不记作精确 0；预留量和实际量不重复累计。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：src/agent_platform/runtime/budgets.py、checks/token_ledger.py。
  - 验证：`.venv/bin/python checks/token_ledger.py` → 两次 usage 累计 22，局部独立；预留与实际不重复；缺 usage 保留未知及来源。
  - 使用环境：本地合成样例／替身；未验收真实模型。
  - 未完成与阻塞：无。
  - 实现决定：TokenLedger 以请求 ID 单次核销，账本分别发布 global/bindings；本卡不实施阻止策略。
  - 下一可执行任务：按索引显式依赖继续。

<a id="i4-t03"></a>

## I4-T03 严格 token 限额

- **目标**：交付可验证的“严格 token 限额”能力。
- **前置任务**：I4-T02、I3-T10、I3-T11。
- **输入依据**：架构第 9.3 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：模型调用前预留、输出限制与响应后核销。
- **范围与约束**：只严格模式；能力不满足就报错，不降级。
- **最小验收**：不足额时本地请求计数仍为 0；支持适配器按剩余量限输出；未知用量失败保留上界并释放未占用额度。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：runtime/budgets.py、runtime/context.py、contracts/models.py、adapters/ollama.py、checks/strict_tokens.py（源码均在 src/agent_platform/）。
  - 验证：`.venv/bin/python checks/strict_tokens.py` → 不足额零调用，剩余输出限额 4；成功核销实际 6；超时保留 26 上界并释放预留；Ollama 严格模式拒绝。
  - 使用环境：本地合成样例／替身；未验收真实模型。
  - 未完成与阻塞：无。
  - 实现决定：严格策略通过可选 token_policy 接入包内模型入口；preflight 明确输入上界；严格支持使用合成适配器验证，默认装配留 T05。
  - 下一可执行任务：按索引显式依赖继续。

<a id="i4-t04"></a>

## I4-T04 非严格 token 限额

- **目标**：交付可验证的“非严格 token 限额”能力。
- **前置任务**：I4-T02、I3-T10、I3-T11。
- **输入依据**：架构第 9.3 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：非严格计量与流转前限额检查。
- **范围与约束**：只非严格模式，估算须注明来源；不声称精确。
- **最小验收**：响应导致超额则下一流转失败且无成功报告；刚好用满且无后续消耗可完成；无法估算明确报错。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：状态=未开始；实际文件=待填写；验证结果=未执行；阻塞=尚未检查；下一步=依赖完成后实施本卡。

<a id="i4-t05"></a>

## I4-T05 预算策略接入

- **目标**：交付可验证的“预算策略接入”能力。
- **前置任务**：I4-T01、I4-T03、I4-T04、I3-T05。
- **输入依据**：架构第 5.3、9 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：将所有预算策略接入图检查入口。
- **范围与约束**：只完成策略装配和配置选择；不实现预算界面。
- **最小验收**：两个节点图不能绕过预算，最后一次包调用可完成；预算失败可终止清理；recursion_limit 另报图限制错误。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：状态=未开始；实际文件=待填写；验证结果=未执行；阻塞=尚未检查；下一步=依赖完成后实施本卡。

<a id="i4-t06"></a>

## I4-T06 预算配置与用量界面

- **目标**：交付可验证的“预算配置与用量界面”能力。
- **前置任务**：I4-T05、I3-T13。
- **输入依据**：架构第 11.1 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：包及任务预算表单、严格开关和用量展示。
- **范围与约束**：只呈现已有策略与计量，不新增限制算法。
- **最小验收**：页面修改限额生成配置版本；运行显示局部/全局量及估算标记，重开表单与保存值一致。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：状态=未开始；实际文件=待填写；验证结果=未执行；阻塞=尚未检查；下一步=依赖完成后实施本卡。

<a id="i4-t07"></a>

## I4-T07 排队任务取消

- **目标**：交付可验证的“排队任务取消”能力。
- **前置任务**：I3-T05、I3-T12。
- **输入依据**：架构第 8.2、10 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：cancel API 的排队路径。
- **范围与约束**：只排队取消及终态幂等；运行任务暂返回尚不支持，不声称完整取消。
- **最小验收**：暂停 worker 后取消，恢复 worker 不执行任务；环境释放；终态重复取消不变；用事件控制一次调度竞争。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：状态=未开始；实际文件=待填写；验证结果=未执行；阻塞=尚未检查；下一步=依赖完成后实施本卡。

<a id="i4-t08"></a>

## I4-T08 运行任务取消

- **目标**：交付可验证的“运行任务取消”能力。
- **前置任务**：I4-T07、I3-T10、I3-T11。
- **输入依据**：架构第 8.2 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：取消标记与 LLM 等待结束路径。
- **范围与约束**：只正常取消等待；错误优先级下一卡完善；取消后不发起后续步骤。
- **最小验收**：本地服务阻塞响应时取消显示等待；放行响应后 cancelled；后续包调用计数为 0；取消先受理不得 completed。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：状态=未开始；实际文件=待填写；验证结果=未执行；阻塞=尚未检查；下一步=依赖完成后实施本卡。

<a id="i4-t09"></a>

## I4-T09 取消等待错误优先级

- **目标**：交付可验证的“取消等待错误优先级”能力。
- **前置任务**：I4-T08。
- **输入依据**：架构第 8.2 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：超时/传输失败与取消的终态判定。
- **范围与约束**：只处理异常优先级及复用既有清理，不实现重试和外部补偿。
- **最小验收**：取消后分别触发超时和断连，均 failed 且保留取消标记；环境解锁；退役实例不被重新激活。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：状态=未开始；实际文件=待填写；验证结果=未执行；阻塞=尚未检查；下一步=依赖完成后实施本卡。

<a id="i4-t10"></a>

## I4-T10 在途任务退出清理

- **目标**：交付可验证的“在途任务退出清理”能力。
- **前置任务**：I1-T09、I4-T09。
- **输入依据**：架构第 2.2、8.2 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：桌面退出时后端传输和图执行清理。
- **范围与约束**：扩展空闲退出处理；不等待 LLM 正常结束。
- **最小验收**：本地模型服务保持等待时退出桌面，连接立即中止且进程按清理时限退出；重启旧 runId 为 404。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：状态=未开始；实际文件=待填写；验证结果=未执行；阻塞=尚未检查；下一步=依赖完成后实施本卡。

<a id="i4-t11"></a>

## I4-T11 任务取消按钮

- **目标**：交付可验证的“任务取消按钮”能力。
- **前置任务**：I4-T09、I3-T13。
- **输入依据**：架构第 11.1 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：任务详情取消交互。
- **范围与约束**：只接入完整 cancel API；区分已受理、等待中、已取消和失败。
- **最小验收**：页面发起取消后先显示等待，随后显示真实终态；失败时显示原始超时原因。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：状态=未开始；实际文件=待填写；验证结果=未执行；阻塞=尚未检查；下一步=依赖完成后实施本卡。

<a id="i4-t12"></a>

## I4-T12 平台闭环演示

- **目标**：交付可验证的“平台闭环演示”能力。
- **前置任务**：I2-T03、I2-T11、I3-T07、I3-T08、I4-T06、I4-T10、I4-T11。
- **输入依据**：架构第 6、7、8、9、10 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：docs/delivery/ 平台演示记录。
- **范围与约束**：只集成验收，不捆绑新增功能；发现问题回到对应卡修复。
- **最小验收**：使用合成模型走配置→更新→提交→回退→查询；排队任务保持旧快照、占用环境不可改、退役实例不能直调；记录 M1 是否通过。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：状态=未开始；实际文件=待填写；验证结果=未执行；阻塞=尚未检查；下一步=依赖完成后实施本卡。
