# 项目协作约定

本文件按项目要求命名为 `AGENT.md`，供参与开发的人和编码助手阅读。不同工具未必会自动加载此文件，开始工作时应显式参考；它不替代工作区上级 `AGENTS.md` 的约束。

## 项目目标与需求依据

- 项目名称：Agent Platform。
- 主项目是通用 Agent 服务平台；`samples/` 下的 Agent 包是附属示例项目。
- 最新明确的用户指令优先于本文档。已确认需求见 `docs/product-requirements.md`，项目状态见 `readme.md`。
- 当前仅完成需求定义，不把建议接口、目录规划或待定技术方案描述为已实现能力。

## 架构原则

1. 包是挂载在中心下的业务服务实现。平台配置其外部依赖并提供模型环境等基础能力。
2. 区分服务包、服务配置和服务实例。一个包版本可形成多个配置不同的实例。
3. 包依赖能力契约，平台绑定能力实现。不得在可复用包中硬编码部署地址、模型密钥、租户或业务身份。
4. API 绑定和流程绑定遵循同一输入输出契约。简单字段映射放在接入层，复杂接入逻辑放在接入流程，业务判断放在包内。
5. 平台核心不出现特定课程、作业、学生或查重算法的业务分支。LMS 适配和查重逻辑放在 `samples/` 对应包中。
6. 为工具、包和对外调用定义可执行的输入输出契约。LLM 输出、外部 API 响应和用户输入都需运行时校验，不能仅依赖类型标注。
7. 从单一契约来源生成或推导静态类型、校验规则与公开 Schema，避免多份手工定义漂移。
8. 每次执行固定包版本和配置版本；配置变更不改变已经开始的任务。
9. 模型调用、工具执行、状态存储和运行记录经由平台提供的接口访问。包声明依赖与能力要求，平台验证并注入。
10. 数据库连接配置作为后续扩展保留。区分平台内部存储与业务数据库访问，不要求简单包接入数据库、RAG 或向量存储。

## 示例包约定

- 每个示例包在 `samples/<package-name>/` 内独立组织，包含业务契约、配置声明、流程、工具、Prompt 和必要示例。
- 包可以包含完整业务实现，但运行依赖由清单声明；复制包不等于复制模型服务、外部 API 或数据库。
- 外部依赖版本及运行时兼容性需要明确，首期按受信任的本地包实现，不宣称具备第三方代码沙箱隔离能力。
- 作业查重示例输出疑似关系与可定位证据。最终复核结论和自动分析结果分开保存；不能把执行失败、未覆盖或证据不足当成未发现问题。
- 不提交真实学生答案、身份凭据或生产接口响应；开发样例使用合成数据或经授权的脱敏数据。

## 实施与验证

- 先跑通最小的挂载、配置、启用和调用闭环，再根据实际包需求提取公共能力。
- 首期无需全量 Apifox 清洗、通用画布、能力市场或多 Agent 运行框架。新增这些范围前应与实际需求对齐。
- 技术栈尚未确定；不要从概念示例推断 Python、TypeScript 或具体框架已经选定。
- 修改契约时同步检查配置表单、绑定映射、包样例和调用方影响。
- 实现后执行与修改相关的轻量验证。契约边界、实例隔离、失败状态和业务证据是优先验证对象。
- 文档修改以内容一致性、链接和 Git 差异检查为主，无需创建镜像实现的测试。
- 完成工作时说明实际变更、已做验证、未实现内容及影响。

## 工作区约束

- 未经用户明确要求，不运行重型测试脚本，不自动打包、构建发布或更新构建产物。
- 依赖包下载和安装、文件权限或属主修改，须事先获得用户明确确认。
- npm 版本升级、发包及发布链路由用户手动操控；不能因实现功能而主动触发。
- 不覆盖无关的用户改动，不擅自重写已有 Git 历史。
- 将配置示例与实际密钥分开，日志和样例不输出认证信息。
- Git 提交沿用当前已配置的用户身份；不擅自修改全局 Git 配置。
