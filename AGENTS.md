# 项目协作约定

适用于本仓库；最新明确的用户指令优先。核对日期：2026-09-24。本文维护协作约束、必要授权与权威入口，产品行为和执行细节由对应手册维护。

## 项目依据与授权

- 项目为通用 Agent 服务平台。范围、决策与未完成边界见 [产品说明](docs/product-requirements.md)，启动见 [项目入口](readme.md)，文档职责见 [文档导航](docs/README.md)。
- 技术栈固定为 FastAPI + Pydantic + LangGraph + PostgreSQL，桌面采用 Electron，包与通用块使用 Python；不得擅自替换。
- 用户明确不考虑多 Agent 与任务断点续跑，不列为后续待办；保留固定服务组合、独立任务并发、节点契约重试和记录持久化。理由见 [产品决策](docs/product-requirements.md#明确不考虑的能力2026-09-21)。
- 业务版本与验收边界以 [知识库出题](examples/black-myth-rag-question-generation/README.md) 和 [文档出题](examples/document-question-generation/README.md) 手册为准；旧版人工通过及模型 pass 不覆盖新版全面内容质量。
- 知识库出题既有授权允许导入 `artifact/负向样本（AI写作）` 的 7 份 DOCX，并使用 ds 验收，累计上限为 100 次，失败与重试也计数。继续调用前必须核对实际最新用量，不按历史记录推断剩余额度；授权与历史计量见 [语义检索验收](docs/archive/2026-09-22/question-semantic-validation.md)。
- [归档](docs/archive/README.md) 中的旧计划、阻塞与授权记录保留历史语境，不自动成为当前任务，也不代表全部验收通过。原查重产品不恢复，保留 artifact。

## 必须保持的架构边界

- 服务页 FlowDraft 是唯一实例配置入口。配置者定义流程、接线及实例，平台校验并编译；模型只能返回契约内数据，不得生成或修改可执行配置、Python 条件代码。控制流与旧协议只读边界见 [控制流手册](docs/control-flow.md)。
- 包只声明严格契约、可选参数和 Prompt，由平台调用模型；无自定义执行入口、依赖或能力列表。数据处理、API 请求和转换使用通用块，业务流程由实例组织。平台核心不得加入课程、学生、查重或固定 LMS 解包分支。
- 用户输入、资源输入输出、LLM 输出及外部 API 响应必须运行时严格校验；静态不兼容阻止保存。从单一契约来源派生类型、校验与 Schema，不通过自动改名、强制转换或兜底制造合规结果。API 请求也须符合明确业务契约；当前校验职责及平台缺口见 [架构说明](docs/architecture-design.md#54-单一契约来源)。
- NodeInput 接收完整主数据与有序参考，字段映射由显式通用块完成。禁止前向、越域或读取其他循环轮次的残留结果。仅可归因的直接生产节点契约错误有界重试，参考、跨字段及容器错误不整体重放；输入、重试、预算和取消机制统一遵守 [架构说明](docs/architecture-design.md)。
- 实例固定实际源码、流程、节点配置及环境引用；任务提交时另固定实际环境设置，不能只保存源路径或静默改写历史。历史回退使用当前环境，不恢复旧地址或凭据。旧实例不能直接接受新调用；固定子服务仅在父快照内执行，组合边界见 [服务组合](docs/service-composition.md)。占用中的环境禁止更新，环境更新不产生实例版本。
- 模型、API、存储和记录经平台接口访问；包不得硬编码部署地址、凭据、租户或业务身份。API 连接与请求路径由块节点配置。一次包尝试计一轮，失败和重试计数；token 按供应商实际用量累计，不估算或预留严格上界，终止清理不受预算阻拦。
- 平台后端管理应用专用 PostgreSQL，桌面管理后端生命周期，独立 CLI 使用同一存储机制。连接与主密钥内部生成，不增加用户数据库配置；仅显式 `--memory` 使用临时存储，启动失败不得降级。退出清理任务、传输和连接后停库，保留数据与密钥，详见 [持久化说明](docs/persistence.md)。
- 不实现任务去重、失败恢复或外部操作回滚。主动取消与应用退出使用不同传输终止语义，按架构处理终态，不因清理重新激活退役实例。
- 本地 embedding/OCR 属于运行资产，不新增资源类别或执行协议。内部存储与业务数据库访问分开，不要求简单包接入数据库、RAG 或向量存储。

## 资源与模板约定

- 包/块只展示同类型、同声明 ID 的最新已导入版本；不得仅按 archived 筛选或在最新版归档后回显旧版。已有固定引用不自动升级；完整版本与归档规则见 [资源手册](docs/resource-guide.md#更新与归档资源)。
- samples 按 blocks/packages/contracts 分类，每类仅保留最小与完整两套模板及共用说明；API 能力纳入 complete.py，不另设第三套功能样例、流程导入协议、契约生成器或业务样例目录。模板必须明确入口、出口、配置及实际支持的钩子。
- 包使用平台已有 StrictModel/Pydantic；复制资源或快照不等于复制远端服务。外部依赖版本和运行兼容性须明确；受信任本地代码不宣称具备第三方权限沙箱。
- 不提交真实学生答案、身份凭据或生产响应，样例采用合成或经授权脱敏数据；失败结果不得伪装成功。

## 实施与验证

- 按当前明确需求选择主要目标、遵守依赖并维护服务配置、环境配置与调用闭环；不按旧任务卡恢复已退役工作。
- 不建设无限自由画布、任意 Python 表达式编辑器、全量 Apifox 治理、能力市场或多 Agent 框架。权限、性能优化、通用防御性框架、兜底及复杂业务规则后置，保留严格契约、明确错误与必要终止逻辑。
- 修改契约时同步检查表单、绑定、模板和调用方。修改规则时更新其权威手册，其余文档保留必要摘要与链接，不复制完整定义或阶段交付流水。
- 优先局部样例、手动演示及必要的少量边界断言，不设覆盖率门槛或新增重型测试平台；替身验证不能替代真实模型验收。重点检查契约、版本、具体错误与平台闭环。
- 文档修改检查内容一致性、本地链接和 Git 差异，无需创建镜像实现的测试。完成时说明实际变更、验证、未实现内容及影响。

## 测试与任务计划的生命周期

- 单个任务、目标或迭代开发期间，可以编写用于确认功能正确性的测试，并维护用于查看需求、步骤和进度的任务计划。
- 对应任务、目标或迭代完成时，删除为其编写的测试文件、测试脚本、临时测试数据和缓存；回归测试也不保留，不将测试文件转存到归档目录。
- 仅为单个业务实例自动加载资源、组装流程、绑定模型或创建服务的辅助脚本（如 `setup_service.py`），不属于业务运行所需的通用块。此类操作统一通过平台页面完成；开发中临时创建的这类脚本在交付清理时删除，不作为长期资源保留，并同步删除其使用命令和文档入口。
- 对应任务计划在完成时移入 `docs/archive/日期/`，不继续作为当前开发入口。归档保留原始需求、实施记录、验证结果和真实完成状态。
- 清理时同步更新文档导航、相对链接和操作命令，移除指向已删除测试的当前运行说明；历史验证记录须明确其历史性质。
- 尚未完成的验收不得标记为通过；若实施计划已被当前使用说明取代而提前归档，必须在当前说明中保留未完成事项及其状态。
- 仍在使用的业务包、通用块、契约、开发模板和使用手册属于交付资源，不作为临时测试或任务计划删除。

## 工作区约束

- 未经用户明确要求，不运行重型测试脚本，不自动打包、构建发布或更新构建产物。
- 依赖包下载和安装、文件权限或属主修改，须事先获得用户明确确认。
- npm 版本升级、发包及发布链路由用户手动操控；不能因实现功能而主动触发。
- 不覆盖无关的用户改动，不擅自重写已有 Git 历史。
- 将配置示例与实际密钥分开，日志和样例不输出认证信息。
- Git 提交沿用当前已配置的用户身份；不擅自修改全局 Git 配置。
