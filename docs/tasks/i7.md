# I7：流程执行与实例版本

- 状态：进行中，4/7 张完成。
- 基线：2026-09-17 产品需求 v0.5、架构 v0.3；旧 I1—I5 仅作为可复用实现及历史证据。
- 必读：[需求](../product-requirements.md)、[设计](../architecture-design.md)、[迭代计划](../iteration-plan.md)、[协作约定](../../AGENT.md)。
- 完成规则：按迭代计划一次领取一张卡，回填真实文件、命令／步骤、结果、模型环境及未完成项；本文件的验收描述是目标，不能当作已执行证据。

<a id="i7-t01"></a>

## I7-T01 顺序拼图编译与节点执行

- **目标**：交付顺序拼图编译与节点执行。
- **前置任务**：I6-T04、I6-T05、I6-T06、I3-T11。
- **输入依据**：架构第 5.2—5.5 节；需求 P01—P09 及第 9 节确认记录中对应规则。
- **交付物**：结构化流程到 LangGraph 的顺序编译、输入引用解析、独立块／包执行入口。
- **范围与约束**：复用现有模型适配与任务上下文；只验证顺序流程，不以共享整份业务状态替代显式端口。
- **最小验收**：输入→块→包→组装→输出可运行；组装可读取多个前序输出；节点输入输出不合法失败；两次任务不共享可变输出。
- **验证方式**：局部样例或手动页面操作，涉及约束／计数时少量定向断言；实施后记录可复现命令或步骤，不预设未存在脚本，不要求全量测试、安装或打包。
- **交接记录**：状态完成；实际文件：src/agent_platform/flows/execution.py、context.py，checks/flow_execution.py；验证：`.venv/bin/python checks/flow_execution.py` → 顺序块／包／多源组装、非法输入输出、两次调用可变数据隔离通过；包能力复用既有 RunContext 与模型适配，控制结构后续接入；使用环境：本地 Python 既有依赖、合成模型／确定性块，无真实模型调用；本卡未完成项：无。下一任务：I7-T02，依赖已满足。

<a id="i7-t02"></a>

## I7-T02 条件分支编译与汇合

- **目标**：交付条件分支编译与汇合。
- **前置任务**：I7-T01。
- **输入依据**：架构第 5.5 节；需求 P01—P09 及第 9 节确认记录中对应规则。
- **交付物**：严格布尔条件块、if/else 容器与共同输出编译。
- **范围与约束**：禁止读取未执行分支；条件输出不做 truthiness 转换，空分支不自动生成输出。
- **最小验收**：true/false 各只执行所选分支；未选分支不调用模型不计预算；两路输出契约不兼容拒绝；循环内分支的结构可被后续循环编译复用。
- **验证方式**：局部样例或手动页面操作，涉及约束／计数时少量定向断言；实施后记录可复现命令或步骤，不预设未存在脚本，不要求全量测试、安装或打包。
- **交接记录**：状态完成；实际文件：flows/execution.py、checks/flow_branches.py；验证：`.venv/bin/python checks/flow_branches.py` 和 `checks/flow_execution.py` → 条件互斥、未选包零调用、非法出口与跨分支引用拒绝、顺序回归通过；实现决定：LangGraph 条件边选择子图，汇合只发布共同出口；使用环境：本地 Python 既有依赖、合成模型／确定性块，无真实模型调用；本卡未完成项：无。下一任务：I7-T03，依赖已满足。

<a id="i7-t03"></a>

## I7-T03 循环编译与携带值

- **目标**：交付循环编译与携带值。
- **前置任务**：I7-T02。
- **输入依据**：架构第 5.3、5.5 节；需求 P01—P09 及第 9 节确认记录中对应规则。
- **交付物**：固定次数 repeat、前置条件 while、显式初值／更新／出口、嵌套作用域。
- **范围与约束**：循环外只能读容器出口；不支持任意回边或 Python 表达式；不重置业务包预算。
- **最小验收**：零次返回初值；固定次数准确；条件为 false 不进循环；达到最大次数仍需继续时报 LOOP_ITERATION_LIMIT；循环内条件和嵌套循环正确；非法反馈拒绝。
- **验证方式**：局部样例或手动页面操作，涉及约束／计数时少量定向断言；实施后记录可复现命令或步骤，不预设未存在脚本，不要求全量测试、安装或打包。
- **交接记录**：状态完成；实际文件：flows/execution.py、checks/flow_loops.py；验证：`.venv/bin/python checks/flow_loops.py` → repeat 准确次数、零次初值、while 前置条件与上限错误、循环内条件及嵌套循环、非法反馈通过；实现决定：每轮独立子作用域，循环外只发布显式携带值出口，不重建任务上下文或预算；使用环境：本地 Python 既有依赖、合成模型／确定性块，无真实模型调用；本卡未完成项：无。下一任务：I7-T04，依赖已满足。

<a id="i7-t04"></a>

## I7-T04 流程控制与累计预算接入

- **目标**：交付流程控制与累计预算接入。
- **前置任务**：I7-T03、I4-T05、I4-T09、I4-T10。
- **输入依据**：架构第 5.3、8、9 节；需求 P01—P09 及第 9 节确认记录中对应规则。
- **交付物**：每个节点和控制流边界的预算／取消检查、循环路径记录及独立图步数限制。
- **范围与约束**：不把图步数等同 loop，不沿用 node_count + 2；不新增自动重试；清理不被限额拦截。
- **最小验收**：循环三次包调用累计三轮；失败尝试计轮；纯块循环受次数限制且可取消；LLM 在途取消等待，退出立即停传输；步数错误与预算错误区分。
- **验证方式**：局部样例或手动页面操作，涉及约束／计数时少量定向断言；实施后记录可复现命令或步骤，不预设未存在脚本，不要求全量测试、安装或打包。
- **交接记录**：状态完成；实际文件：flows/execution.py、runtime/context.py、contracts/runs.py、checks/flow_control.py；验证：`.venv/bin/python checks/flow_control.py` → 三轮累计、失败计轮、路径、纯块取消、在途等待及退出取消传输、独立图步数及清理通过；`checks/cancel_running.py`、`checks/cancel_errors.py` → 既有本地 HTTP 取消正常／超时／断连回归通过；图步数使用独立 10000 默认上限且跨子图累计；使用环境：本地 Python 既有依赖、合成模型／确定性块，无真实模型调用；本卡未完成项：无。下一任务：I7-T05，依赖已满足。

<a id="i7-t05"></a>

## I7-T05 拼图实例快照保存

- **目标**：交付拼图实例快照保存。
- **前置任务**：I6-T07、I7-T04、I2-T06。
- **输入依据**：架构第 4、6 节；需求 P01—P09 及第 9 节确认记录中对应规则。
- **交付物**：服务保存接收完整拼图、编译并固定模块／契约／参数／环境引用／样例，原子生成实例。
- **范围与约束**：保存不执行业务调用；固定内容不只存模块路径；编译器版本纳入快照。
- **最小验收**：完整拼图含纯块流程保存成功；非法端口或无效包节点保存失败且无半成品实例；修改源文件不改变旧内容；有效输入样例出现在服务 Schema。
- **验证方式**：局部样例或手动页面操作，涉及约束／计数时少量定向断言；实施后记录可复现命令或步骤，不预设未存在脚本，不要求全量测试、安装或打包。
- **交接记录**：状态未开始；实际文件及验证证据尚无。前置任务完成后领取，不以本次文档更新标记实现完成。

<a id="i7-t06"></a>

## I7-T06 拼图版本与历史回退

- **目标**：交付拼图版本与历史回退。
- **前置任务**：I7-T05、I2-T10。
- **输入依据**：架构第 4、6.2 节；需求 P01—P09 及第 9 节确认记录中对应规则。
- **交付物**：流程结构和模块内容的版本分类；历史读取完整拼图及配置；回退、复制历史到草稿。
- **范围与约束**：环境自身更新不升实例版本；同会话内历史，不保留旧入口双轨兼容。
- **最小验收**：拓扑／契约／块内容改变升大版本；只改节点参数或环境绑定升小版本；内容与配置同改记 breaking；回退恢复原实例且使用当前环境；失败不切指针。
- **验证方式**：局部样例或手动页面操作，涉及约束／计数时少量定向断言；实施后记录可复现命令或步骤，不预设未存在脚本，不要求全量测试、安装或打包。
- **交接记录**：状态未开始；实际文件及验证证据尚无。前置任务完成后领取，不以本次文档更新标记实现完成。

<a id="i7-t07"></a>

## I7-T07 完整实例任务调用

- **目标**：交付完整实例任务调用。
- **前置任务**：I7-T06、I3-T12。
- **输入依据**：架构第 7、8、10 节；需求 P01—P09 及第 9 节确认记录中对应规则。
- **交付物**：运行提交使用生成实例、固定流程及环境；查询包含节点／循环路径／输出／错误／用量。
- **范围与约束**：稳定入口、expectedInstanceId 和无去重语义保留；不开放退役实例直接调用。
- **最小验收**：草稿不可提交；纯块实例无需模型环境可执行；更新服务不改变在途任务；环境占用保持；失败不发布部分报告；重启旧 runId 不存在。
- **验证方式**：局部样例或手动页面操作，涉及约束／计数时少量定向断言；实施后记录可复现命令或步骤，不预设未存在脚本，不要求全量测试、安装或打包。
- **交接记录**：状态未开始；实际文件及验证证据尚无。前置任务完成后领取，不以本次文档更新标记实现完成。

