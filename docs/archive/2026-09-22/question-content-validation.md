# 出题内容与内部溯源隔离修复（2026-09-22）

用户反馈 essay-3 要求比较材料发布状态并说明如何限定来源，答案要求填写 fragmentId 和逐字 quote。原始任务 `run_5e5a94289c134635b6910ba63146a517` 的需求只有“根据黑神话材料，只出三道论述题”，未要求考查引用规范。

## 根因与历史纠正

规划把模型自身的冲突处理规则变成了考点；生成把内部引用格式变成了参考答案与评分点。审题已经指出评分点“并非材料内容”，却降为 advisory 后给出 pass；平台当时仅检查证据归属、引文、题型数量、题号覆盖及审题结构，未拒绝元数据进入考生内容。审题也未直接读取原始需求，而是以规划为标准，因此错误相互传递。

复查此前两道论述任务 `run_a78f1a5ca20145e7bc3055d502e79340`，发现参考答案也含片段哈希。原数量与执行验证仍有效，但此前将模型 pass 作为内容质量通过的证据不充分，已在原验收记录、当前手册和需求说明中更正。历史任务和原始审题结果不改写。

## 修复与离线验证

全部 8 个包与 19 个块升级至 4.0.0。规划、生成、审题和修订 Prompt 明确：内部证据字段仅写入结构化 evidence；不得要求考生操作切分标识，不得为凑题把引用规范或冲突处理当作考点。审题和修订直接读取原始需求；偏题、平台操作要求、无材料依据的评分点和实质重复赋分必须阻断。

确定性校验覆盖规划考点/目标及考生可见的题干、选项、答案、解析、纠正和评分点。拒绝内部字段名以及当前证据中的片段、文档、版本、知识库标识和哈希；不检查 evidence 正常溯源字段。校验在规划/单题处理及最终结果上执行，不能由模型 pass 绕过。命中后显式失败，不通过删除字符掩盖坏题。

8 项轻量拒绝验证通过，包含用户原始完整结果、题干/答案/评分点中的 fragmentId、fragment_id、revisionId、实际片段哈希和文档 ID，以及错误 pass 最终结果；正常材料内容与结构化引用通过，27 个资源加载和契约副本检查通过。自然语言偏题仍需要模型审题与人工判断，不宣称规则完全证明语义正确。

## 正式服务

通过 Electron 页面导入并逐节点替换资源、保留接线/参考/运行预算、切换新版路由和条件块，完整静态校验通过并保存为 6.0。服务 `svc_303e8991d0124b4eb92de3fbc0b4edc3` 的当前实例为 `ins_d9e6e211d4014ee8b40017d858d4dba9`。旧任务固定快照不变。

## 同需求真实复验

任务 `run_06913141efcd4dc080a50dda544566e4` 使用正式 6.0，需求原样为“根据黑神话材料，只出三道论述题”，同一知识库修订及默认检索范围。规划回到游戏基本信息、材料描述的影响及产业展望。

初审在 essay-3 发现跨题重复赋分，以及将“材料认为/有理由相信/期待”等归因话术作为得分要求，均列为 blocking，给出 revise。系统只定向修订 essay-3，essay-1/2 所有字段保持不变；删除重复文化影响赋分和指定归因话术要求，改为依据材料评价和产业展望作答，复审 pass、findings=[]，最终业务 completed。三题数量与题号完整，最终确定性核验通过；正文/答案/评分点无内部字段或证据标识。仍保留“区分材料展望与已发生事实”的内容性评分，不要求填写来源 ID。

8 次模型调用（解析 1、规划 1、生成 3、初审 1、修订 1、复审 1），输入 43,948、输出 33,276，共 77,224 token。服务历史含用户自行提交任务，复验前 39 次、复验后 47 次，未超过 100 次授权；此前报告的 32 次为当时统计，不包含随后新增的用户任务。本次真实证明了阻断→定向修订→复审路径，不证明全部语义错误均能自动识别；真实重出与两轮耗尽仍未验证，未宣称已获新的用户人工验收。

去除环境凭据快照的完整本地记录位于忽略目录 artifact/black-myth-rag-content-2026-09-22.json，不提交仓库。临时测试与页面操作脚本交付时删除；未安装依赖、构建或发布产物。
