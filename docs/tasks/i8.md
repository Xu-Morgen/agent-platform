# I8：配置页面、样例迁移与验收

- 状态：进行中，1/9 张完成。
- 基线：2026-09-17 产品需求 v0.5、架构 v0.3；旧 I1—I5 仅作为可复用实现及历史证据。
- 必读：[需求](../product-requirements.md)、[设计](../architecture-design.md)、[迭代计划](../iteration-plan.md)、[协作约定](../../AGENT.md)。
- 完成规则：按迭代计划一次领取一张卡，回填真实文件、命令／步骤、结果、模型环境及未完成项；本文件的验收描述是目标，不能当作已执行证据。

<a id="i8-t01"></a>

## I8-T01 服务顺序拼图页面

- **目标**：交付服务顺序拼图页面。
- **前置任务**：I6-T07。
- **输入依据**：架构第 11.1 节；需求 P01—P09 及第 9 节确认记录中对应规则。
- **交付物**：模块库、输入／输出契约选择、顺序节点插入移动删除、端口接线与前序输出引用、草稿操作。
- **范围与约束**：原生 Electron 页面复用现有 IPC；本卡不宣称条件／循环交互完成。
- **最小验收**：无需写完整 JSON 即可搭建两个块的流程；不兼容连接显示原因；显式转换块可接；前序输出可引用；未完成草稿可编辑。
- **验证方式**：局部样例或手动页面操作，涉及约束／计数时少量定向断言；实施后记录可复现命令或步骤，不预设未存在脚本，不要求全量测试、安装或打包。
- **交接记录**：状态完成。
  - 实际文件：`desktop/renderer/flow-editor.js`、`index.html`、`index.js`、`style.css`；定向页面检查 `desktop/checks/flow-editor.cjs`。
  - 验证：`env -u ELECTRON_RUN_AS_NODE desktop/node_modules/.bin/electron desktop/checks/flow-editor.cjs` → PASS；真实页面构造两块顺序流程，错接显示节点原因、插入转换修复、前序输出引用、移动删除及不完整草稿保存重载通过；两份 JS 的 `node --check` 通过。
  - 使用环境：真实 Electron + 本地 Python 后端、确定性块，无模型调用。
  - 未完成与阻塞：本卡无；包配置、条件循环、实例保存按后续卡交付。
  - 实现决定：契约与来源使用目录下拉；移动/删除保留失效引用并报错，后端预检为权威判断；替换旧手写实例编辑入口。
  - 下一可执行任务：I8-T02，依赖满足。

<a id="i8-t02"></a>

## I8-T02 业务包悬浮配置窗

- **目标**：交付业务包悬浮配置窗。
- **前置任务**：I8-T01、I6-T05、I6-T06。
- **输入依据**：架构第 5.7、11.1 节；需求 P01—P09 及第 9 节确认记录中对应规则。
- **交付物**：点击包节点的参数表单、预算、环境连接选择、字段说明与错误、节点状态回写。
- **范围与约束**：修复上一轮第 2 项；通用块没有模型配置窗；复杂参数可局部 JSON 编辑，不能以完整实例 JSON 代替页面。
- **最小验收**：同包两节点独立配置；修改一个不影响另一个；未配置节点明显无效；环境下拉完成实际绑定；不用手改 capabilityBindings；错误定位字段。
- **验证方式**：局部样例或手动页面操作，涉及约束／计数时少量定向断言；实施后记录可复现命令或步骤，不预设未存在脚本，不要求全量测试、安装或打包。
- **交接记录**：状态未开始；实际文件及验证证据尚无。前置任务完成后领取，不以本次文档更新标记实现完成。

<a id="i8-t03"></a>

## I8-T03 条件与循环拼图交互

- **目标**：交付条件与循环拼图交互。
- **前置任务**：I8-T01、I7-T03。
- **输入依据**：架构第 5.5、11.1 节；需求 P01—P09 及第 9 节确认记录中对应规则。
- **交付物**：if/else、repeat、while 容器，条件块、分支出口、次数上限和循环携带值编辑，嵌套展示。
- **范围与约束**：不提供任意 Python 编辑器或无限制自由回边；采用已确认控制结构。
- **最小验收**：页面可搭建并校验两分支、零次循环、条件循环及嵌套；缺少最大次数或反馈类型不兼容时定位；越作用域连线被拒绝。
- **验证方式**：局部样例或手动页面操作，涉及约束／计数时少量定向断言；实施后记录可复现命令或步骤，不预设未存在脚本，不要求全量测试、安装或打包。
- **交接记录**：状态未开始；实际文件及验证证据尚无。前置任务完成后领取，不以本次文档更新标记实现完成。

<a id="i8-t04"></a>

## I8-T04 实例保存及历史页面

- **目标**：交付实例保存及历史页面。
- **前置任务**：I8-T02、I8-T03、I7-T06。
- **输入依据**：架构第 6、11.1 节；需求 P01—P09 及第 9 节确认记录中对应规则。
- **交付物**：草稿保存与实例版本保存区分；实例标识、完整历史拼图、节点配置查看及回退。
- **范围与约束**：不需先加载 instance.json；不提供额外发布步骤；旧历史证据不冒充新 UI 验收。
- **最小验收**：合法拼图保存即生效；未配置包不能保存实例；错误定位节点；修改参数生成新版本；历史只读、复制编辑和回退可操作。
- **验证方式**：局部样例或手动页面操作，涉及约束／计数时少量定向断言；实施后记录可复现命令或步骤，不预设未存在脚本，不要求全量测试、安装或打包。
- **交接记录**：状态未开始；实际文件及验证证据尚无。前置任务完成后领取，不以本次文档更新标记实现完成。

<a id="i8-t05"></a>

## I8-T05 任务页与节点执行展示

- **目标**：交付任务页与节点执行展示。
- **前置任务**：I8-T04、I7-T07。
- **输入依据**：架构第 10、11.1 节；需求 P01—P09 及第 9 节确认记录中对应规则。
- **交付物**：选择已保存实例对应服务、合法输入样例、状态及循环尝试、错误、取消、结果与用量展示。
- **范围与约束**：保留稳定服务入口，不允许直接调用退役版本；不新增教师业务页面。
- **最小验收**：填入样例可通过输入校验；纯块及含包实例可调用；显示当前固定实例；失败指出节点与具体原因；可取消；任务页不包含流程拼装。
- **验证方式**：局部样例或手动页面操作，涉及约束／计数时少量定向断言；实施后记录可复现命令或步骤，不预设未存在脚本，不要求全量测试、安装或打包。
- **交接记录**：状态未开始；实际文件及验证证据尚无。前置任务完成后领取，不以本次文档更新标记实现完成。

<a id="i8-t06"></a>

## I8-T06 新版模块与拼图模板

- **目标**：交付新版模块与拼图模板。
- **前置任务**：I8-T05。
- **输入依据**：架构第 5.6、12、13 节；需求 P01—P09 及第 9 节确认记录中对应规则。
- **交付物**：迁移 samples/template，提供包、单文件块、契约资源及顺序／条件／循环拼图样例；新接入说明。
- **范围与约束**：不保留旧实例脚本入口兼容要求；更新旧操作说明；模型可用明确标注的本地替身。
- **最小验收**：从模板复制改标识，经模块加载、页面拼图、配置保存到调用闭环通过；至少一个纯块流程和一个同包双节点不同配置样例。
- **验证方式**：局部样例或手动页面操作，涉及约束／计数时少量定向断言；实施后记录可复现命令或步骤，不预设未存在脚本，不要求全量测试、安装或打包。
- **交接记录**：状态未开始；实际文件及验证证据尚无。前置任务完成后领取，不以本次文档更新标记实现完成。

<a id="i8-t07"></a>

## I8-T07 查重拼图迁移

- **目标**：交付查重拼图迁移。
- **前置任务**：I8-T06、I5-T01、I5-T02、I5-T03、I5-T04。
- **输入依据**：架构第 12 节；需求 P01—P09 及第 9 节确认记录中对应规则。
- **交付物**：清洗、计分、输入适配、报告组装单文件块，复用语义包与契约，查重拼图及操作说明。
- **范围与约束**：本卡不改变整段精确匹配算法、不新增判抄袭布尔值；替身验收明确标注，不算 M2。
- **最小验收**：通过服务拼图生成实例；组装显式引用前序定量及定性结果；0/0.5/1 计分和双报告通过；语义失败无部分成功；无写死会话环境 ID。
- **验证方式**：局部样例或手动页面操作，涉及约束／计数时少量定向断言；实施后记录可复现命令或步骤，不预设未存在脚本，不要求全量测试、安装或打包。
- **交接记录**：状态未开始；实际文件及验证证据尚无。前置任务完成后领取，不以本次文档更新标记实现完成。

<a id="i8-t08"></a>

## I8-T08 新版平台闭环验收

- **目标**：交付新版平台闭环验收。
- **前置任务**：I8-T05、I8-T06、I7-T04。
- **输入依据**：架构第 6—11 节；需求 P01—P09 及第 9 节确认记录中对应规则。
- **交付物**：新版 M1 页面操作记录与少量受影响边界验证，说明替身与真实部分。
- **范围与约束**：不运行无关全量检查，不打包发布；所有本卡场景完成才标记新版 M1。
- **最小验收**：从全新会话加载单文件块和包、配置环境、拼顺序／条件／循环、包弹窗配置、保存调用；拒绝错接并经转换修复；版本回退及在途隔离、具体错误、取消和退出通过。
- **验证方式**：局部样例或手动页面操作，涉及约束／计数时少量定向断言；实施后记录可复现命令或步骤，不预设未存在脚本，不要求全量测试、安装或打包。
- **交接记录**：状态未开始；实际文件及验证证据尚无。前置任务完成后领取，不以本次文档更新标记实现完成。

<a id="i8-t09"></a>

## I8-T09 新版真实查重验收

- **目标**：交付新版真实查重验收。
- **前置任务**：I8-T07、I8-T08。
- **输入依据**：架构第 12；需求第 8 节 节；需求 P01—P09 及第 9 节确认记录中对应规则。
- **交付物**：通过新拼图实例完成约 1500 字 1 对 6 的真实模型报告、实际用量及模式记录；新版 M2 证据。
- **范围与约束**：承接旧 I5-T08 未完成的真实验收目标；不得以替身或仅旧脚本链路替代；不评价模型语义准确率。
- **最小验收**：真实适配器返回 completed、定量及定性报告齐全，记录局部／全局 loop 与 usage；分享前审阅脱敏；缺环境明确阻塞。
- **验证方式**：局部样例或手动页面操作，涉及约束／计数时少量定向断言；实施后记录可复现命令或步骤，不预设未存在脚本，不要求全量测试、安装或打包。
- **交接记录**：状态未开始；实际文件及验证证据尚无。前置任务完成后领取，不以本次文档更新标记实现完成。

