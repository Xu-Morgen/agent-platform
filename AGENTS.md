# 项目协作约定

本文件是 Codex 默认发现的项目级指令文件，统一维护项目背景、架构、工作区约束及测试与任务计划的生命周期。适用于本仓库；最新明确的用户指令优先。

## 项目目标与需求依据

- 项目为通用 Agent 服务平台。当前能力和未完成边界见 [产品说明](docs/product-requirements.md)，启动见 [项目入口](readme.md)，文档职责与导航见 [docs/README.md](docs/README.md)。协作指令只保留长期约束及必要授权，不追加交付流水。
- 唯一实例配置入口为服务页 FlowDraft，当前执行协议为 flow-6。flow-5 及更早实例只读；通过页面重建并保存新实例，固定子服务引用不自动升级。统一迁移说明见 [控制流手册](docs/control-flow.md#协议升级)。
- 桌面及独立 CLI 默认管理应用专用本地 PostgreSQL；已验证 Ubuntu 24.04 生命周期，配置和任务历史跨重启保留。依赖纳入 uv.lock；启动及恢复边界见 [持久化说明](docs/persistence.md)。
- 用户于 2026-09-21 明确不考虑多 Agent 与任务断点续跑，两者不列为后续待办；保留固定服务组合、独立任务并发、节点契约重试及记录持久化。理由集中在 [产品决策](docs/product-requirements.md#明确不考虑的能力2026-09-21)。
- 已交付 PDF/DOCX 知识库、受控读取与证据、平台本地 embedding/OCR。模型属于运行资产，不增加资源类别或执行协议；资源及旧服务必须显式升级，见 [知识库](docs/knowledge.md)、[embedding](docs/local-embedding.md)、[OCR](docs/local-ocr.md)。
- 知识库出题当前版本、公开结果及验收边界统一在 [业务手册](examples/black-myth-rag-question-generation/README.md)。考点重叠仍可能被审题降为 advisory，未完成全面内容质量验收；旧固定三题人工通过不覆盖后续版本。旧全文出题服务 2.0 及后续资源升级尚未完成新版真实模型复验，见 [实例手册](examples/document-question-generation/README.md)。
- 知识库出题既有授权允许导入 `artifact/负向样本（AI写作）` 的 7 份 DOCX，并使用 ds 验收，累计调用上限已由 30 提高为 100 次。最近记录为服务历史含用户任务累计 74/100 次（2026-09-22）；2026-09-24 输出整理未增加调用。这是记录时点，继续调用前须核对实际最新用量，不能按旧数值推断剩余额度；失败与重试也计数。历史授权与验证见 [语义检索验收](docs/archive/2026-09-22/question-semantic-validation.md)。
- 已完成或退役计划、阶段验收及旧产品资料集中在 [历史归档](docs/archive/README.md)，不按历史阻塞状态限制新工作，也不把归档等同于验收通过。原查重产品、旧入口及临时测试已移除，保留 artifact；samples 三类模板及控制流教学说明属于长期交付。

## 架构原则

固定版本服务组合及契约选择规则见 [服务组合](docs/service-composition.md)。服务节点固定叶子实例，首版只开放一层并禁止自引用；同一父任务统一管理环境、预算、取消和记录，不整体重试子流程。父快照内部执行固定退役子实例不等于向该子实例接受新调用。契约共享身份仅用于可证明相同的结构与校验规则，不静默改写历史引用。

模型输入输出必须由声明契约严格校验。流程、接线与实例由配置者定义，平台验证并编译；if/else 和条件循环由 Python 脚本块判断，可读取模型输出中已声明的类型、枚举或字段。模型不得生成或修改流程、脚本、接线或实例，不将模型输出作为可执行配置。

1. 包是挂载在中心下的可复用 LLM 交互单元，仅维护输入输出数据结构验证、可选参数和 Prompt；平台统一替换字段并调用模型，转换和数据处理由通用块节点完成；完整业务入口和流程归实例。平台配置外部依赖并提供基础能力。
2. 区分业务包、服务配置、服务实例和稳定服务入口。服务页拼接模块并完成配置后保存生成实例；可组合多个包，也可只包含通用块。包可被多个实例复用，同包不同节点配置独立。
3. 包不声明执行入口、代码依赖或能力列表；包节点只绑定模型连接；外部 API 连接和请求路径由声明 API 能力的通用块节点配置，请求路径不写死在可复用块源码中。不得在可复用包中硬编码部署地址、模型密钥、租户或业务身份。
4. 业务包和通用块的输入输出契约由源码或清单固定声明。节点输入机制为：主数据自动接收上一层完整输出（服务第一步取完整服务输入），简单模式参考上一节点当次 primary，高级模式可替换为作用域内已执行节点完整输出等合法来源的有序参考列表；平台统一封装 primary/references，严格校验参考数量、位置与类型，不递归保留参考封装。具体控制边界见 [架构说明](docs/architecture-design.md#13-flow-6-入口与历史边界)。当前执行协议为 flow-6，资源入口为 NodeInput[P, tuple[...]]；旧协议历史只读，不能执行或回退激活。平台不提供业务字段选取、改名或合并，字段映射和结构转换必须由通用块完成；服务返回只配置输出契约，自动校验并返回顶层流程最后一步的完整输出，不提供返回数据来源选择；不保留旧映射、旧输入协议或旧 if 引用的兼容执行逻辑，也不得静默重写历史快照。API 绑定和流程绑定均保留严格契约，节点封装不替代 API 请求/响应业务契约。固定格式解包、清洗和验证通过明确输入输出的 Python 通用流程块完成；需要语义判断的映射由模型完成，业务流程由实例组织。
5. 平台核心不出现特定课程、作业、学生或查重算法的业务分支。业务逻辑应放在独立块和包中；当前 samples 仅交付通用开发模板，不恢复已删除的查重产品。平台不再内置固定 LMS 解包，具体接口响应由业务通用块处理。
6. 为工具、包和对外调用定义可执行的输入输出契约。LLM 输出、外部 API 响应和用户输入都需运行时校验，不能仅依赖类型标注。
7. 从单一契约来源生成或推导静态类型、校验规则与公开 Schema，避免多份手工定义漂移。
8. 每次提交任务固定生成的实例流程、包／块内容、配置及实际环境；配置更新生成新实例，成功保存立即生效，不改变既有任务。包变更视为大版本、配置变更视为小版本，同时变更视为破坏性整体更新。
9. 模型调用、工具执行、状态存储和运行记录经由平台提供的接口访问。包只声明契约和 Prompt，平台保留模型请求/响应以及业务输入输出的严格校验。
10. 稳定服务入口指向当前实例；旧实例不可直接接受新调用，需从中心选择历史版本回退。完整快照不能仅保存源路径；PostgreSQL 模式持久化源码字节与已规范化拼图。
11. 环境变更不改变实例版本，引用该环境的排队、运行或取消等待任务未结束时禁止更新。
12. 一次包调用尝试计一轮 loop，失败和重试也计数。节点和任务管理 loop/token 限额，包可通过 budgetDefaults 提供节点初始化建议，预算不与业务 Config 耦合，每次状态流转前检查，token 按供应商实际用量累计并检查，不提供严格模式、预留上界或估算，终止清理不被预算拦截。
13. 平台内部数据库固定由桌面管理，连接与主密钥内部生成，不增加用户连接配置。业务数据库连接作为后续扩展保留。区分平台内部存储与业务数据库访问，不要求简单包接入数据库、RAG 或向量存储。

## 示例包约定

- 包和块只展示同类型、同声明 ID 的最新已导入版本；资源列表、管理区、插入选择、条件块候选和契约来源均不展示旧版。不要仅按 archived 筛选，也不要因最新版归档而回显旧版；已有实例/草稿的固定引用保留，不自动升级。

- 当前示例按 `samples/blocks/`、`samples/packages/`、`samples/contracts/` 分类，每类只提供最小与最完整两套示例，共用说明文档保留；通用块的 API 能力纳入 complete.py，不另设功能示例文件。接入步骤在 samples/USAGE.md。每类明确入口出口、配置字段和实际支持的钩子，不引入另一套流程导入协议、契约生成器或业务样例目录。
- Prompt 包无需声明运行依赖；契约使用平台已有 StrictModel/Pydantic，实例组合包、配置与流程。复制包或保存实例快照不等于复制模型服务、外部 API 或数据库。
- 外部依赖版本及运行时兼容性需要明确，首期按受信任的本地包实现，不宣称具备第三方代码沙箱隔离能力。
- 原查重实例的输入输出及判断边界留在 `docs/archive/2026-09-18/product-requirements.md` 中，当前不作为模板交付要求。所有模型样例失败都不能伪装为成功报告。
- 不提交真实学生答案、身份凭据或生产接口响应；开发样例使用合成数据或经授权的脱敏数据。

## 实施与验证

- 维护已交付的服务配置、环境配置与调用闭环；samples 保持三类资源最小/完整模板，不恢复已移除的查重产品。
- 服务拼图保持严格类型约束；if/while 条件为严格 bool，repeat/while 有限执行，foreach/switch 遵守 [控制流手册](docs/control-flow.md)。不建设无限制自由画布、任意 Python 表达式编辑器、全量 Apifox 治理、能力市场或多 Agent 框架。
- 当前 if 与 while 均独立执行资源开发者编写的 Python 条件块，读取统一节点输入并返回严格 bool；条件不替换业务主数据。高级参考不得前向、越域或读取其他循环轮次的残留结果；模型不得生成或修改条件代码。参考错误与完整入口跨字段错误立即失败；仅可归因的直接生产节点契约错误按当前架构重试，不重跑无关历史生产节点。
- 技术栈由用户确定为 FastAPI + Pydantic + LangGraph + PostgreSQL，桌面端采用 Electron 简易实现；包与通用块使用 Python。桌面及独立 CLI 默认管理本地 PostgreSQL，只有显式 --memory 用于内存开发；禁止因数据库启动失败自动退回内存。
- 平台由合法拼图编译实例 LangGraph 图；节点入口出口须静态兼容并运行时校验，不匹配要显式转换块。业务包点击后配置参数、预算和环境；平台包装节点负责预算和取消检查。图步数不等于包调用 loop，服务可配置额外重试次数 retryLimit（0～1000，默认 3）；运行时输出不符合本步骤输出契约或直接下游 primary 字段契约且可归因于生产节点时，下一步不得执行，重试该生产节点，耗尽后报错；参考、完整入口跨字段错误和容器错误不整体重放。每次尝试计预算并检查取消；静态类型不兼容阻止保存。按用户明确决定，不建设断点续跑所需的持久化 checkpointer 或失败任务恢复。
- Electron 主进程管理 Python 后端，后端管理专用 PostgreSQL；页面通过 preload/IPC 访问 FastAPI。桌面退出先清理任务和传输、关闭连接，再停库，保留 app.getPath("userData")/storage 下的数据与密钥。
- 内存模式保持会话语义；PostgreSQL 模式恢复配置、历史和任务记录，未完成任务标记 APPLICATION_INTERRUPTED，不重入队或重放外部操作。
- 权限服务、性能优化、通用防御性框架、兜底及复杂业务规则后置；保留严格契约、明确错误和必要任务终止逻辑。
- 主动取消等待当前 LLM 请求传输结束才成功；关闭应用则立即停止本地模型传输并中止任务，两者语义不同。
- 首期不实现去重键，每次成功提交创建新任务，不按输入内容复用任务。
- 取消等待超时或传输失败优先报错并记 failed；正常取消记 cancelled。两者清理临时上下文、恢复实例就绪，保留任务终态，不回滚外部操作或重新激活退役实例。
- 桌面程序退出立即停止本地传输；桌面历史跨重启保留，开发用 --memory 除外。预算组合按当前架构设计执行。
- 开发按用户当前明确需求选择一个主要目标，遵守依赖并更新相关说明；`docs/archive/` 为历史记录，不要求沿用旧卡回填工具。
- 首期优先快速交付：默认局部样例、手动演示和必要的少量边界断言，不设覆盖率门槛，不新增重型测试平台；替身结果不能替代真实模型验收。
- 修改契约时同步检查配置表单、绑定映射、包样例和调用方影响。
- 实现后执行与修改相关的轻量验证。严格输入输出契约、实例版本、具体错误与平台闭环是优先验证对象；模板验证包括加载、配置、接线及必要边界，不沿用已删除查重产品的验收数据要求。
- 文档修改以内容一致性、链接和 Git 差异检查为主，无需创建镜像实现的测试。
- 完成工作时说明实际变更、已做验证、未实现内容及影响。

## 测试与任务计划的生命周期

- 单个任务、目标或迭代开发期间，可以编写用于确认功能正确性的测试，并维护用于查看需求、步骤和进度的任务计划。
- 对应任务、目标或迭代完成时，删除为其编写的测试文件、测试脚本、临时测试数据和缓存；回归测试也不保留，不将测试文件转存到归档目录。
- 仅为单个业务实例自动加载资源、组装流程、绑定模型或创建服务的辅助脚本（如 `setup_service.py`），不属于业务运行所需的通用块。此类操作统一通过平台页面完成；开发中临时创建的这类脚本在交付清理时删除，不作为长期资源保留，并同步删除其使用命令和文档入口。
- 对应任务计划在完成时移入 `docs/archive/日期/`，不继续作为当前开发入口。归档保留原始需求、实施记录、验证结果和真实完成状态。
- 清理时同步更新文档导航、相对链接和操作命令，移除指向已删除测试的当前运行说明；历史验证记录须明确其历史性质。
- 尚未完成的验收不得标记为通过；若实施计划已被当前使用说明取代而提前归档，必须在当前说明中保留未完成事项及其状态。
- 仍在使用的业务包、通用块、契约、开发模板和使用手册属于交付资源，不作为临时测试或任务计划删除。

## 工作区约束

- 未经用户明确要求，不运行重型测试脚本，不自动打包、构建发布或更新构建产物。
- 依赖包下载和安装、文件权限或属主修改，须事先获得用户明确确认。
- npm 版本升级、发包及发布链路由用户手动操控；不能因实现功能而主动触发。
- 不覆盖无关的用户改动，不擅自重写已有 Git 历史。
- 将配置示例与实际密钥分开，日志和样例不输出认证信息。
- Git 提交沿用当前已配置的用户身份；不擅自修改全局 Git 配置。
