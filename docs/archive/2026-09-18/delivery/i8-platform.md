# I8 新版 M1 页面闭环记录

> 归档日期：2026-09-18。保留当时的需求、设计或交接记录；文中的完成状态、命令和接口可能已过时，不作为当前开发要求。当前入口见 [文档导航](../../../README.md)。

> 历史交接记录：本页记录当时的实现及验证，不是当前使用入口。2026-09-17 用户已确认最小产品验证完成；精简后旧测试命令、数据和入口已移除。当前状态见 [迭代索引](../../../README.md)，操作见 [项目入口](../../../../readme.md)。

日期：2026-09-17。结论：新版 M1 通过，I8-T01～T08 完成；M2 真实模型验收另见 I8-T09。

环境：Linux 图形会话、Electron 44.4.1、项目现有 Python 3.12.3 虚拟环境；真实 Electron renderer/preload/IPC/FastAPI/LangGraph。模型端点全部为本地 HTTP 协议替身，输入合成，无真实凭据、无真实模型语义结论。没有安装依赖、构建或运行无关全量检查。

以下页面检查均使用前缀 `env -u ELECTRON_RUN_AS_NODE desktop/node_modules/.bin/electron desktop/checks/`，逐个执行对应文件：

| 文件 | 本次实际操作与结果 |
| --- | --- |
| `platform-flow.cjs` | 全新会话，通过环境页获取模型并保存环境；服务页加载标准包，同包两节点独立参数/预算/环境，缺绑定定位字段；拼顺序保存 1.0；任务暂停于真实 HTTP 传输时编辑保存 1.1、复制历史、回退 1.0。原任务保持原实例，两节点请求上限仍为 64/128，完成双调用、global loop=2；环境更新返回 ENVIRONMENT_IN_USE。未配置新节点拒绝保存，历史不增加。PASS |
| `flow-editor.cjs` | 页面加载转换/布尔块，错接显示节点错误；移动显式转换块后可接，前序输出引用、删除及不完整草稿重载。PASS |
| `flow-controls.cjs` | 页面搭建 if 两分支、嵌套零次 repeat、带前置条件的 while 和显式携带值；保存后任务页调用，条件假时 completed，条件持续真时报 LOOP_ITERATION_LIMIT；缺上限及反馈类型错误定位，内部节点不能作为外层来源。PASS |
| `flow-tasks.cjs` | 纯块及含包实例使用已校验输入样例；循环两轮显示路径、节点输出和实际用量；模型输出失败无成功报告；页面取消等待当前传输，放行后 cancelled。PASS |
| `flow-exit.cjs` | 真实应用主入口关闭窗口，运行/排队任务退出，阻塞模型传输关闭，后端在 2 秒内正常退出；新进程查询两个旧 runId 均 404。PASS |

模板从复制改标识到页面加载、拼图、样例、保存、调用的证据为 I8-T06 已实际执行的 `template-page.cjs`；纯块/条件/循环与同包双配置的模板调用证据为 `checks/template_copy.py`。这些记录使用新版拼图，不引用 I4 的旧手写实例验收充当新版证据。

本次闭环发现并修复两点：就绪提示提前于环境/目录初始化，可能覆盖用户开始填写的表单；历史来源中的显式 nodeId=null 与页面省略该字段不一致，可能误显示不可用引用。现已延后就绪提示并规范来源显示。

限制：所有服务、草稿、历史、任务与凭据仅当前会话内存；不包含安装包、持久化、真实模型 1 对 6 验收。
