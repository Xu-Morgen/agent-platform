# 正文预览修复记录

2026-09-22 用户反馈正文预览候选全部报必填字段缺失或旧协议不兼容。

## 原因与交付

管理页此前列出所有服务，却仅提交 `{knowledge, versionId}`。正式环境中三个服务属于 flow-5，知识库出题服务还需要 `requirement`，没有专用正文预览服务。

新增预览候选接口，按当前协议、输入输出契约和环境配置筛选；无候选时禁用预览并提示创建步骤，单个候选自动选中，提交时固定预期实例。旧服务和历史快照没有改写。

通过正式服务页面保存“DOCX 正文预览”1.0，使用准备文档预览、读取 DOCX 段落两个普通块，无模型绑定。服务 ID：`svc_7edbc5cd501d4601ad4d949248e18125`；实例 ID：`ins_499a0c2fd7e64d44956ea8813b211ae0`。

## 验证及边界

- 使用正式环境实际服务 Schema 验证筛选：仅正文预览匹配；旧协议、额外必填 requirement/limits、错误输出和缺少知识库标记均排除。
- 通过 Electron 页面桥接提交真实知识库文档，任务 `run_1ed91bed9370469ca766a6f31d18a1f0` 完成，返回 28 个片段，首定位 `paragraph:1:characters:1-64`，没有模型调用。
- 新路由空候选 HTTP 契约、JavaScript 语法及 Git 差异检查通过。正式服务和真实任务验证完成后，原桌面进程已退出，复查旧端口连接被拒绝。新筛选接口与页面组合需重新打开应用后加载；尚未进行新版筛选页面整体验收。
- 临时 Electron、验证脚本与测试数据交付时清理，不保留测试套件。
