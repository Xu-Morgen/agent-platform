# 项目协作约定

本文件按项目要求命名为 `AGENT.md`，供参与开发的人和编码助手阅读。不同工具未必会自动加载此文件，开始工作时应显式参考；它不替代工作区上级 `AGENTS.md` 的约束。

## 项目目标与需求依据

- 项目名称：Agent Platform。
- 主项目是通用 Agent 服务平台；首期同时交付平台产品和 `samples/` 下包含标准模板与最小查重能力的业务包产品。
- 最新明确的用户指令优先于本文档。已确认需求见 `docs/product-requirements.md`，项目状态见 `readme.md`。
- 2026-09-17 新基线为产品需求 v0.5、架构 v0.3：服务页拼图生成完整实例，单文件通用块、逐包节点悬浮配置、类型约束端口、顺序／条件／循环。允许纯通用块实例，无模型预算或环境要求；显式引用前序输出，结构转换必须用块；不要求兼容旧手写实例入口。
- I1—I4 和 I5-T01～T07 的完成记录仅证明旧基线；I5-T08 原真实验收阻塞目标由 I8-T09 承接。I6、I7 均已完成 7/7 张，I8 已完成 1/9 张，新版 M1/M2 待验收。下一可领取 I8-T02；以 `docs/iteration-plan.md` 为唯一状态索引。后端已使用完整拼图保存及执行协议，旧桌面页面与业务脚本待 I8 迁移，不能用旧调用方式验证新版。启动见 readme.md。

## 架构原则

1. 包是挂载在中心下的可复用 LLM 交互单元，仅封装模型交互前后的输入输出、Prompt 与必要处理；完整业务入口和流程归实例。平台配置外部依赖并提供基础能力。
2. 区分业务包、服务配置、服务实例和稳定服务入口。服务页拼接模块并完成配置后保存生成实例；可组合多个包，也可只包含通用块。包可被多个实例复用，同包不同节点配置独立。
3. 包依赖能力契约，平台绑定能力实现。不得在可复用包中硬编码部署地址、模型密钥、租户或业务身份。
4. API 绑定和流程绑定遵循同一输入输出契约。固定格式解包、清洗和验证通过明确输入输出的 Python 通用流程块完成；需要语义判断的映射由模型完成，业务流程由实例组织。
5. 平台核心不出现特定课程、作业、学生或查重算法的业务分支。查重逻辑放在 `samples/` 对应单文件业务块及语义交互包中；固定 LMS 消息格式处理可作为平台通用块。
6. 为工具、包和对外调用定义可执行的输入输出契约。LLM 输出、外部 API 响应和用户输入都需运行时校验，不能仅依赖类型标注。
7. 从单一契约来源生成或推导静态类型、校验规则与公开 Schema，避免多份手工定义漂移。
8. 每次提交任务固定生成的实例流程、包／块内容、配置及实际环境；配置更新生成新实例，成功保存立即生效，不改变既有任务。包变更视为大版本、配置变更视为小版本，同时变更视为破坏性整体更新。
9. 模型调用、工具执行、状态存储和运行记录经由平台提供的接口访问。包声明依赖与能力要求，平台验证并注入。
10. 稳定服务入口指向当前实例；旧实例不可直接接受新调用，需从中心选择历史版本回退。完整内容快照只存内存，不能仅保存源路径。
11. 环境变更不改变实例版本，引用该环境的排队、运行或取消等待任务未结束时禁止更新。
12. 一次包调用尝试计一轮 loop，失败和重试也计数。包声明可配置 loop/token 限额，每次状态流转前检查，token 严格执行可配置，终止清理不被预算拦截。
13. 数据库连接配置作为后续扩展保留。区分平台内部存储与业务数据库访问，不要求简单包接入数据库、RAG 或向量存储。

## 示例包约定

- 新版示例在 `samples/<sample-name>/` 内组织 `packages/`、`blocks/` 与 `flows/`，区分模型交互包、单文件确定性块和拼图／契约样例；现有 instance/ 脚本按 I8 迁移，不要求旧入口兼容。
- 包声明运行依赖；实例组合包、配置与流程。复制包或保存实例快照不等于复制模型服务、外部 API 或数据库。
- 外部依赖版本及运行时兼容性需要明确，首期按受信任的本地包实现，不宣称具备第三方代码沙箱隔离能力。
- 查重实例首期输入目标纯文本与对照纯文本数组，输出按重复段落文本确定性计算的定量相似度报告和模型生成的定性报告。教师自行判断是否抄袭，首期不实现复核结论存储或 AI 最终布尔判定。失败不能伪装为成功报告。
- 不提交真实学生答案、身份凭据或生产接口响应；开发样例使用合成数据或经授权的脱敏数据。

## 实施与验证

- 首期交付服务配置页、环境配置页、Python 通用流程块、标准包／实例模板与最小查重产品，优先跑通挂载、配置保存立即生效与调用闭环。
- 首期必须实现受类型约束的服务拼图、if/else、固定次数及有最大次数限制的条件循环；条件由严格布尔通用块提供。不建设无限制自由画布、任意 Python 表达式编辑器、全量 Apifox 治理、能力市场或多 Agent 框架。
- 技术栈由用户确定为 FastAPI + Pydantic + LangGraph + PostgreSQL，桌面端采用 Electron 简易实现；包与通用块使用 Python。PostgreSQL 持久化仍在后续数据库版本落地，首期仅内存，不描述为已安装或实现。
- 平台由合法拼图编译实例 LangGraph 图；节点入口出口须静态兼容并运行时校验，不匹配要显式转换块。业务包点击后配置参数、预算和环境；平台包装节点负责预算和取消检查。图步数不等于包调用 loop，首期不启用持久化 checkpointer、自动重试或恢复。
- Electron 主进程管理 Python 后端子进程，页面通过 preload/IPC 访问 FastAPI；桌面退出需清理后端进程及本地传输。
- 配置历史、任务与报告首期只存应用缓存，回退限同一应用运行期间；重启不恢复任务或历史，后续随数据库能力持久化到本地。
- 权限服务、性能优化、通用防御性框架、兜底及复杂业务规则后置；保留严格契约、明确错误和必要任务终止逻辑。
- 主动取消等待当前 LLM 请求传输结束才成功；关闭应用则立即停止本地模型传输并中止任务，两者语义不同。
- 首期不实现去重键，每次成功提交创建新任务，不按输入内容复用任务。
- 取消等待超时或传输失败优先报错并记 failed；正常取消记 cancelled。两者清理临时上下文、恢复实例就绪，保留任务终态，不回滚外部操作或重新激活退役实例。
- 桌面程序退出立即停止本地传输；首期历史只存内存，数据库版本才支持跨重启回退。定量细则与预算组合按架构设计执行。
- 开发按 `docs/iteration-plan.md` 索引领取 `docs/tasks/` 中的一张任务卡，每次只交付一个主要目标；遵守显式依赖，完成后更新索引状态和任务卡交接记录。
- 首期优先快速交付：默认局部样例、手动演示和必要的少量边界断言，不设覆盖率门槛，不新增重型测试平台；替身结果不能替代真实模型验收。
- 修改契约时同步检查配置表单、绑定映射、包样例和调用方影响。
- 实现后执行与修改相关的轻量验证。严格输入输出契约、实例版本、具体错误与平台闭环是优先验证对象；以约 1500 字文本的 1 对 6 比较验证可用性，不验证 LLM 语义准确性。
- 文档修改以内容一致性、链接和 Git 差异检查为主，无需创建镜像实现的测试。
- 完成工作时说明实际变更、已做验证、未实现内容及影响。

## 工作区约束

- 未经用户明确要求，不运行重型测试脚本，不自动打包、构建发布或更新构建产物。
- 依赖包下载和安装、文件权限或属主修改，须事先获得用户明确确认。
- npm 版本升级、发包及发布链路由用户手动操控；不能因实现功能而主动触发。
- 不覆盖无关的用户改动，不擅自重写已有 Git 历史。
- 将配置示例与实际密钥分开，日志和样例不输出认证信息。
- Git 提交沿用当前已配置的用户身份；不擅自修改全局 Git 配置。
