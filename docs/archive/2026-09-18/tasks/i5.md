# I5：查重产品交付任务卡

> 归档日期：2026-09-18。保留当时的需求、设计或交接记录；文中的完成状态、命令和接口可能已过时，不作为当前开发要求。当前入口见 [文档导航](../../../README.md)。

> 历史交接记录：本页记录当时的实现及验证，不是当前使用入口。2026-09-17 用户已确认最小产品验证完成；精简后旧测试命令、数据和入口已移除。当前状态见 [文档导航](../../../README.md)，操作见 [项目入口](../../../../readme.md)。

> 历史任务：以下状态及证据对应旧基线，不表示服务拼图新需求已完成。新版实施按 [I6—I8 索引](../iteration-plan.md)执行。 I5-T08 保留原阻塞记录，真实模型验收目标由 I8-T09 承接；不再单独领取旧入口验收。

- 状态：已完成 7/8 张；I5-T08 因缺少可用真实模型环境阻塞，M2 未完成。
- 需求映射：P01、P05、P09、业务需求第 6 节。
- 执行规则与统一完成标准：[迭代总计划](../iteration-plan.md)。
- 必读：[协作约定](../../../../AGENTS.md)、[需求](../product-requirements.md)、[架构](../architecture-design.md)。

每张卡只有一个主要交付目标；为该目标补充接口适配、错误处理和轻量验证仍属于同一任务。按显式依赖执行，不要求为了完成早期卡而提前实现后续能力。

<a id="i5-t01"></a>

## I5-T01 查重报告契约

- **目标**：交付可验证的“查重报告契约”能力。
- **前置任务**：I1-T03。
- **输入依据**：架构第 12 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：样例实例输入及最终报告模型。
- **范围与约束**：只定义结构、索引和数值约束；不计算分值、不调用模型。
- **最小验收**：合法报告通过；空数组、错误元素、越界数值与重复/缺失对照索引被拒绝。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：samples/assignment-similarity/instance/contracts.py、examples/{input,report-contract}.json、checks/similarity_contracts.py。
  - 验证：`.venv/bin/python checks/similarity_contracts.py` → 正常样例及 18 类定向拒绝断言通过；双方同时遗漏末尾对照亦拒绝。
  - 使用环境：人工契约样例，未计算分值、未调用模型。
  - 未完成与阻塞：无。
  - 实现决定：原请求对照数量通过 validation context 传入；无上下文时至少要求连续完整索引；所有模型继承 StrictModel。
  - 下一可执行任务：I5-T02，依赖已满足。

<a id="i5-t02"></a>

## I5-T02 文本预处理

- **目标**：交付可验证的“文本预处理”能力。
- **前置任务**：I5-T01。
- **输入依据**：架构第 12.1 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：instance/ 清洗与分段函数。
- **范围与约束**：只做换行、NFC、水平空白和空段处理；不近似匹配。
- **最小验收**：合成输入得到固定段落；空白目标和任一空白对照返回原字段路径；标点与大小写保留。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：samples/assignment-similarity/instance/preprocessing.py、checks/similarity_preprocessing.py。
  - 验证：`.venv/bin/python checks/similarity_preprocessing.py` → 固定段落与 NFC、换行、水平空白、空段处理通过；目标及第 2 个对照空白保留原字段路径；大小写标点保留。
  - 使用环境：本地纯函数合成输入，无模型。
  - 未完成与阻塞：无。
  - 实现决定：每个 LF 分隔非空行视为段落，使用 CONTRACT_VALIDATION_ERROR 返回原输入字段路径；复核修正只折叠水平空白，段内垂直制表符与 Unicode 行分隔符不改写，定向断言已通过。
  - 下一可执行任务：I5-T03，依赖已满足。

<a id="i5-t03"></a>

## I5-T03 重复段落计分

- **目标**：交付可验证的“重复段落计分”能力。
- **前置任务**：I5-T02。
- **输入依据**：架构第 12.2 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：instance/ 定量模块。
- **范围与约束**：只确定性计分及匹配索引，不调用 LLM。
- **最小验收**：完全相同=1、无匹配=0、两等长段落匹配一段=0.5；重复对照段落不放大分值。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：samples/assignment-similarity/instance/scoring.py、checks/similarity_scoring.py。
  - 验证：`.venv/bin/python checks/similarity_scoring.py --artifact-dir artifact` → 1/0/0.5、重复位置和 Unicode 计数通过；现有 7 篇 DOCX 提取后 1 对 6 完成，自比较为 1。
  - 使用环境：合成断言及用户 artifact 本地文本；无模型，不提交原文。
  - 未完成与阻塞：无。
  - 实现决定：分母不计换行；对照重复索引全部列出但每个目标位置只计一次；未使用 AIGC 标签评估查重。
  - 下一可执行任务：I5-T04，依赖已满足。

<a id="i5-t04"></a>

## I5-T04 语义分析包

- **目标**：交付可验证的“语义分析包”能力。
- **前置任务**：I5-T01、I3-T11、I4-T05。
- **输入依据**：架构第 12.3 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：packages/semantic/ 清单、Prompt 与包入口。
- **范围与约束**：只一次语义交互，不包含完整查重流程；不输出抄袭布尔值。
- **最小验收**：本地模型样例返回对齐索引的结构；缺项、重复索引及错误类型失败；一次尝试计一轮。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：samples/assignment-similarity/packages/semantic/、sync_contracts.py、checks/similarity_{semantic,support}.py。
  - 验证：`.venv/bin/python samples/assignment-similarity/sync_contracts.py --check && .venv/bin/python checks/similarity_semantic.py` → 平台实际 Ollama 适配器连接本地 HTTP 协议替身；合法输出及缺项、重复、错误类型、额外布尔判定验证通过；每次尝试含失败均 loop=1。
  - 使用环境：本地 HTTP 模型替身，非真实模型；真实验收属于 T08。
  - 未完成与阻塞：无。
  - 实现决定：Prompt 从包快照读取；契约从 instance/contracts.py 生成入独立包并校验同步，避免双源维护；全对照一次调用，无重试。
  - 下一可执行任务：I5-T05，依赖已满足。

<a id="i5-t05"></a>

## I5-T05 完整查重图

- **目标**：交付可验证的“完整查重图”能力。
- **前置任务**：I5-T03、I5-T04、I3-T05。
- **输入依据**：架构第 12.3 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：instance/ 查重 LangGraph 定义和配置。
- **范围与约束**：只组合已有清洗、计分与语义包，两类报告必须同时成功。
- **最小验收**：本地模型完成样例得到两类报告；语义步骤失败时不发布定量部分为成功。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：samples/assignment-similarity/instance/{models,workflow,entry}.py、instance.json、checks/similarity_workflow.py。
  - 验证：`.venv/bin/python checks/similarity_workflow.py` → 四节点图返回 0.5/0 定量和对齐定性报告；loop=1、token=48（替身）；语义缺项或 HTTP 失败无成功结果；空白路径保留；严格不支持零请求。
  - 使用环境：平台实际适配器连接本地 HTTP 协议替身，非真实模型。
  - 未完成与阻塞：无。
  - 实现决定：实例保留默认 strictTokenLimit=true；Ollama 验证显式配置 false，不自动降级；最终组装再次按原请求数目验证。
  - 下一可执行任务：I5-T06，依赖已满足。

<a id="i5-t06"></a>

## I5-T06 标准模板整理

- **目标**：交付可验证的“标准模板整理”能力。
- **前置任务**：I5-T05、I2-T05。
- **输入依据**：架构第 5 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：samples/template/ 可复制包和独立实例样例。
- **范围与约束**：只整理成可复用模板，不新增平台协议。
- **最小验收**：复制并更换包/实例标识，组合两包两配置可加载执行；平台源码无需改动。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：samples/template/、checks/template_copy.py。
  - 验证：`.venv/bin/python samples/template/sync_contracts.py --check && .venv/bin/python checks/template_copy.py` → 临时目录复制并更换两包及实例标识；两配置输出限额 11/17 独立生效，第二包接收第一包结果；全局 loop=2、局部各 1。
  - 使用环境：本地 HTTP Ollama 协议替身；无需安装依赖或修改平台源码。
  - 未完成与阻塞：无。
  - 实现决定：默认同包双绑定可复制为异包组合；模板契约单源生成，包与实例目录可独立挂载。
  - 下一可执行任务：I5-T07，依赖已满足。

<a id="i5-t07"></a>

## I5-T07 接入操作说明

- **目标**：交付可验证的“接入操作说明”能力。
- **前置任务**：I5-T06、I4-T12。
- **输入依据**：架构第 2、5、11 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：readme 与 samples/ 接入说明。
- **范围与约束**：只记录已存在的启动、加载、配置、调用和退出步骤。
- **最小验收**：依据说明从干净进程启动并执行合成样例；命令、路径和内存丢失说明与实现一致。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：完成。
  - 实际文件：readme.md、AGENT.md、samples/{README.md,invoke_similarity.py}、samples/assignment-similarity/README.md、checks/similarity_onboarding.py。
  - 验证：`.venv/bin/python checks/similarity_onboarding.py` → 干净真实后端进程通过 HTTP 创建环境、加载、保存、提交和查询双报告；退出码 0；重启原 runId 404 且服务列表为空。
  - 使用环境：全新 Python 后端进程及本地 HTTP 模型替身；不代表真实模型验收。
  - 未完成与阻塞：无。
  - 实现决定：接入脚本只调用已有 API；实例加载前需在临时副本替换环境占位符；只显式导出报告，不增加持久化。
  - 下一可执行任务：I5-T08，依赖已满足。

<a id="i5-t08"></a>

## I5-T08 真实查重验收

- **目标**：交付可验证的“真实查重验收”能力。
- **前置任务**：I5-T07。
- **输入依据**：架构第 12、13 节；读取前置任务的实际文件和交接记录，不假设其未记录的能力已存在。
- **交付物**：约 1500 字的 1 对 6 合成输入及脱敏报告记录。
- **范围与约束**：只真实模型验收；需要可用地址和凭据，不以替身冒充；不评估语义准确率。
- **最小验收**：使用实际适配器完成一轮并同时返回定量、定性报告，记录实际模式与用量；无环境则标记阻塞，不能宣布 M2 完成。
- **验证方式**：优先复用已有样例、一次本地调用或手动演示；涉及计数、校验和竞态时用少量定向断言。无全量测试、压力测试或打包要求。
- **完成证据**：在本卡回填实际文件、执行命令或操作步骤、关键结果；替身标明为替身。未验证不标记完成。
- **交接记录**：
  - 状态：阻塞。
  - 实际文件：samples/assignment-similarity/examples/acceptance-1v6.json、checks/similarity_acceptance_input.py、docs/delivery/i5-similarity.md；复用 samples/invoke_similarity.py。
  - 验证：`.venv/bin/python checks/similarity_acceptance_input.py` → 合成 1 对 6 输入及确定性计分通过，每篇约 1560 字；`curl -sS --max-time 3 http://127.0.0.1:11434/api/tags` → 连接失败，退出码 7。
  - 使用环境：仅本地确定性输入验证；未调用真实模型，无真实双报告和实际用量。
  - 未完成与阻塞：缺少实际模型 API 地址、模型名及必要凭据。已按用户要求补充 OpenAI 兼容远程 API（openai-chat），无需本机 Ollama；本地协议替身验证通过，尚无真实双报告。M2 未完成。
  - 实现决定：验收输入为独立合成数据，不将用户 artifact 原文发送给外部模型；优先使用 openai-chat 远程适配器，也保留 ollama-chat；均需显式非严格模式，不以替身冒充。
  - 下一可执行任务：取得真实模型环境后执行交付记录中的 1 对 6 命令，审阅脱敏报告、模式和 usage，再更新完成状态。
