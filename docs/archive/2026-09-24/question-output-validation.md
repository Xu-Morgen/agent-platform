# 知识库出题最终输出精简

日期：2026-09-24。当前操作入口为 [业务手册](../../../examples/black-myth-rag-question-generation/README.md)。

用户要求最终回复只保留题目、答案、得分点等交付内容，不携带完整知识库与语义检索信息。

新增普通转换块 `rag-question-present-result@1.0.0`（`blocks/present_result.py`）：入口使用现有完整 Result，重新执行原有结论校验，出口为该块定义的 PublicResult。内部契约仍由原同步工具嵌入；未修改既有生成、审题和修订资源。平台核心无业务分支或字段映射新增。

成功输出保留题号、题型、题干、选择题选项、答案、解析、判断题纠正和论述题得分点/分值；移除 State、知识库、检索候选、证据引用、规划与审题历史。非成功结论只返回原状态与原因，不展示未通过的题目。内部资料仍保留在任务步骤、证据与检索记录中。

验证：

- 使用现有真实任务结果，经真实块子进程验证三种题型。公开题目逐字段等于原题目去掉 evidence 后的内容，未改写答案或得分点；覆盖混合题型与部分题型。
- 最新任务示例的 JSON 序列化长度由 98,950 减至 12,388（默认 ASCII 转义口径）；仅用于说明删除内部资料的效果。
- 资料不足、不支持请求使用历史结果验证；质量未达标使用合成两轮阻断状态验证。仅保留状态和原因。
- 未审题状态被明确拒绝，不通过整理块绕过原有审核。
- 输出 Schema 不包含 State、EvidenceContext、Review、Request 或 Citation。
- 契约副本一致性与 Git 差异检查通过。

本轮没有远端模型调用，没有重新生成题目，也不改变既有内容质量结论（包括考点重叠尚未解决的边界）。历史任务结果不重写。

正式页面操作通过：加载新块、在顶层末尾添加节点、高级参考置空、选择公开输出契约、预检及保存。正式服务 `svc_303e8991d0124b4eb92de3fbc0b4edc3` 从 7.0 升至 8.0，实例 `ins_df856a4c603a4c82be1ec66e8b2860a7`。保存后读回确认最后节点为整理块、空参考，服务返回 Schema 无内部状态/检索定义。没有重新运行整个出题流程。

临时验证脚本、历史结果临时副本和独立桌面缓存已删除；保留交付资源、手册与本记录。
