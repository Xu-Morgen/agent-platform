# 知识库出题数量升级验收（2026-09-22）

本记录为本次交付的历史验证摘要。当前操作入口见 [业务手册](../../../examples/black-myth-rag-question-generation/README.md)。

## 需求与交付

仅支持单选、判断、论述，取消各一道限制；允许任意正总数、单一题型或部分题型组合，同类数量合并。其他题型、负数量、空需求和总数为零仍明确拒绝。题目 ID 按题型连续编号；规划、生成、汇总、审题、缺项、修订及最终结果必须覆盖本次请求，不默默补题或截断。

8 个业务包和 19 个块均为 3.0.0，契约副本检查一致。正式服务通过 Electron 页面导入、重组、校验并保存，未通过脚本直接构造或提交实例。服务 `svc_303e8991d0124b4eb92de3fbc0b4edc3` 更名为“知识库出题 · ds”；当前 5.2 实例为 `ins_9861394135c34605b66f83a31332fb53`。既有任务快照不变。

页面 foreach 上限 1,000，节点调用预算 1,000，任务调用预算 1,007，任务及节点 token 预算 10,000,000，单次输出上限 8,192，最多两轮修订。这些可调执行限额不构成业务契约题量上限；过大的题量仍可能因预算、输出长度或证据不足无法完成。

## 离线与页面验证

16 项轻量边界验证通过：单题、判断四题、论述两题、混合五题、同类需求合并、省略零数量题型、10,001 题展开；不支持题型、空需求、零总数及负数量；完整混合流程、漏题、仅审前三题、未知发现或缺项 ID、定向修订与重出后耗尽。全部 27 个业务资源及四个独立契约加载通过。正式页面完整接线与新版契约校验通过。

任务页继续自动显示知识库下拉框；业务 JSON 只需 requirement，知识库引用在提交时补齐并固定修订。

## 真实 ds 验证

任务 `run_48a6d15495bf4d508ec186d6aeca1e51` 使用固定 5.0 验收快照，需求为“根据黑神话材料的文化传播、技术创新与产业影响，出2道单选题和2道判断题，不需要论述题。”使用此前授权的七份 DOCX、当前知识库修订及默认 8 个选用片段/12,000 字符检索参数。

平台状态与业务状态均 completed；输出 choice-1、choice-2、judgment-1、judgment-2，未额外生成论述题。审题完整覆盖四题，verdict=pass，无阻断问题，保留一条关于不同材料“成功”与“尚未正式发布”表述口径差异的 advisory。该建议未被隐藏，不把材料内容视为已核实事实。

本轮 7 次真实调用（解析 1、规划 1、逐题 4、审题 1），输入 22,873、输出 13,127，共 36,000 token。连同旧版的 20 次，共 27 次，未超过当时用户授权的 30 次；验收任务全局预算固定 10 次。无新资料导入和额外模型探测。脱离凭据的完整本地记录在忽略目录 artifact/black-myth-rag-counts-2026-09-22.json，不提交仓库。

数量升级后的真实修订/重出、两轮耗尽及极大题量未做远端验收；相关边界仅离线验证。用户此前人工通过对应旧三题版本，不宣称新版已获再次人工验收。

用户在四题验收完成后将累计真实调用授权上限提高至 100 次；追加单一题型的两道论述题验证，实际用量另列于下文。

追加任务 `run_a78f1a5ca20145e7bc3055d502e79340` 使用正式 5.2 实例，请求“根据黑神话材料，只出2道论述题，围绕文化传播和游戏产业影响，不需要单选题和判断题。”平台与业务状态均 completed，严格返回 essay-1、essay-2，审题完整覆盖且 pass。5 次调用（解析 1、规划 1、逐题 2、审题 1），输入 18,838、输出 19,033，共 37,871 token。本次升级两项真实任务共 12 次、73,871 token，连同旧版累计 32 次、306,975 token，未超过更新后的 100 次授权。完整脱离凭据的记录在忽略目录 artifact/black-myth-rag-essay-counts-2026-09-22.json。

临时验证和页面操作脚本已清理，不随业务资源交付；未安装依赖或触发构建发布。正式配置、任务记录及授权原件保存在桌面 PostgreSQL 数据目录。
